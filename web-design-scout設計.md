# web-design-scout スキル 設計書

`web-design-mock` / `web-design-distill` に並ぶ**第3のスキル**(実例 → 出典付き比較ギャラリー +
違和感の診断)の設計判断と根拠を残す。運用には不要。実装の正は
`.claude/skills/web-design-scout/SKILL.md`(本書執筆時点では未作成 = フェーズ B で起こす)。

> 対になる文書: `web-design設計.md`(哲学 → モック)/ `web-design-distill設計.md`(モック → 哲学)。
> 本書はその語彙・作法(フェーズ間ゲート / `00-state.md` baton / progressive disclosure /
> **値を捏造しない** / 自己完結 HTML / 機械チェック)を引き継ぐ前提で書く。重複は再掲しない。

> **本書は C フェーズ(試し打ち)を経て書かれている。** `web-design-scout/_rubrics/table.md` と
> `web-design-scout/table-list-shot/`(gallery.html / sources.md)が実走の根拠。各節の「C で確認」は
> その実測。

---

## 1. 目的

既存2スキルは2つの入口を持つ ―「哲学 → モック」「モック → 哲学」。本スキルは**第3の入口**を足す:

> **実例 → モック。** 実プロダクトの定石 UI を偵察し、出典付きで複数パターンを横並びにし、
> **現行デザインとの違和感を言語化する。**

解く痛みは具体的: **「モックに違和感はあるが、明確に指摘できない」**。
ギャラリーを見せるだけでは「どっちが好きか」しか言えない。効くのは
**現行モックを実例と同じ軸で並べ、ズレた軸を一語で言う診断**。これが本スキルの主価値。

既存が「1枚を深く磨く(深さ)」なのに対し、本スキルは「定石を複数並べて見比べる(広さ)」。
ループの形からして別物 ―だから独立スキルにする(§2)。

---

## 2. 形態の判断 — 独立スキル + 完全な責務分離

ユーザー要件で**最重要の制約**: 「既存とは**完全に責務を分離**して作りたい」。これを設計の柱に据えた。

| 観点 | 判断 | 根拠 |
|---|---|---|
| スキル形態 | **独立スキル**(mock/distill と兄弟) | ループが広さ優先で別物。親を肥大化させない(§親の設計価値) |
| 出力先 | **独立 `web-design-scout/<slug>/`** | `web-design-mock/` を汚さない。発見・状態が混ざらない |
| スクリプト | **自前の軽量チェッカ**を持つ | mock の `scripts/` に依存しない(パス結合を作らない) |
| ルーター/references/provenance | **mock 側を一切触らない** | `referenced` クラスは scout 内に閉じる。authoritative/empirical の棚を汚さない |
| 連携 | **手渡しのみ**(自動連携ゼロ) | 採用カードはユーザーが mock 側へ手で移植。スキル間の自動呼び出しを作らない |

> distill は「親ルーターに発見ステップを足す」= **親への変更を伴う**接続だった。scout は逆に
> **接続を作らない**ことが要件。distill との対比で「責務分離の徹底度」がこのスキルの個性。

---

## 3. 核心の課題と 3 原則(設計の出発点)

authoritative 哲学は公式が値を保証し、empirical 哲学は実測+D4 検証で保証する。
**実プロダクトには、参照できる正確な値が無い**(トークン非公開)。放置すると
「スクショを目分量で写した、出典も精度も曖昧なコピー」に堕する。これが本スキル最大のリスク。

### 原則1 — 完全再現は不可能。"コピー" でなく "出典付き解釈"
実プロダクトはトークンを公開しない。**C で実証**: Primer(一次)も Setproduct(記事)も
**正確な px を出さない**(両者とも「自分の type scale に対して目で設計しろ、px を発明するな」と明言)。
→ 再現値は**実測でも捏造でもない第3状態 = `推定 estimated`** として必ずラベルする。
ブランドアセットは転載せず、中身はダミーデータに置換(模写参考 + 出典明示の立て付け)。

### 原則2 — 診断は「観察できる作法」だけで行う(推定値に依存しない)
曖昧な hex/px ではなく、**目で確認できる作法**で語る ―「数値は右寄せか」「罫線かゼブラか枠か」
「密度モードはどれか」「操作は常時表示か hover か」。**C で実証**: これらの作法は出典で裏取りできる
(数値右寄せ+tabular-nums=Primer 明文 / 横罫>ゼブラ=Ström / 密度3モード=Setproduct)。
→ 診断は地に足が付く。**値を出さず作法で語る**のが鉄則(「#fafafa にしろ」ではなく「ゼブラをやめ横罫のみに」)。

### 原則3 — 二次でモック、一次で根拠(役割分担)
ユーザーは「二次(実プロダクト)優先」を選択 ― 実在感が主価値だから。だが**C で発見**: 一次
(Primer 等のデザインシステム公式)を**作法の裏取り**に併用すると診断の根拠が固くなる。
→ **二次=モックの見た目 / 一次=診断軸の根拠**。二次優先は維持しつつ一次を捨てない。

> この 3 原則が、本スキルを「スクショ写経ツール」と分ける点。原則1=正直さ、原則2=診断の地盤、
> 原則3=精度の補強。

---

## 4. ループの形 — 広さ優先のファンアウト(滝ではない)

mock/distill は 4 フェーズの**ウォーターフォール**(深さ)。scout は**ファンアウト**(広さ)。
フェーズ間ゲートの作法は引き継ぐが、構造は「1枚を磨く」ではなく「並べて見比べる」。

| # | フェーズ | 内容 | 成果物 |
|---|---|---|---|
| S1 | **スコープ** | パターン(テーブル/フィルタ/課金表…)+ 粒度(コンポーネント or 画面)+ 枚数(既定3) | `00-scout-state.md` |
| S2 | **偵察(並列 web 研究)** | 実プロダクトを二次優先で集める。一次で作法を裏取り | `sources.md`(出典台帳) |
| S3 | **再現** | 各候補を**同一ダミーデータ**で自己完結 HTML カード化。出典 + provenance + 推定ラベル付き | `gallery.html` |
| S4 | **診断** | 現行モックがあれば語彙テーブルで各軸を値づけし、ズレた軸を言語化 | `gallery.html` 内 or `diagnosis.md` |
| S5 | **手渡し** | 採用候補を提示。移植はユーザーが本体 mock 側で | — |

- **粒度は両対応**(ユーザー選択): コンポーネント単位(テーブル等)も画面アーキタイプ(設定画面等)も。
- **S2 は並列エージェントが効く**(各プロダクトを別エージェントが偵察)。ただし Workflow は
  ユーザーの明示 opt-in が要る ― 既定は逐次 web 研究、規模が出たら提案。
- **同一ダミーデータが肝**(C で実証): 中身を揃えると作法差だけが際立ち、純粋比較になる。

---

## 5. 診断ルーブリック(本書の核 — 一番の発明)

「違和感を言葉に変える**統制語彙**」。`web-design-scout/_rubrics/<pattern>.md` に置く。

### 5.1 なぜ「軸 × 値の候補」か
自由記述だと診断がブレる。**軸を固定し、各軸の値を観察で選ぶ**ことで、現行と実例を同じ物差しで
並べられる。テーブルの例(`_rubrics/table.md`、C で初出): 密度モード / 行高体感 / 区切り戦略
(罫線なし・横罫・ゼブラ・全枠・カード)/ 罫線濃度 / 数値整列 / ヘッダ作法 / ステータス表現 /
行アクション / ホバー / 先頭列 / 空状態 / ページネーション ― **12 軸**。

### 5.2 診断アウトプットの型
```
■ 現行モック:  C=ゼブラ / E=左寄せ / H=常時表示
■ Stripe(実例): C=横罫のみ / E=右寄せtabular / H=hoverケバブ
─────────────────────────────────
差分(違和感の正体): C・E・H の3軸
  → 詰まり/雑然の主因は「ゼブラ(C)」「アクション常時表示(H)」。
  → 金額が読みにくいのは「左寄せ(E)」。右寄せ+tabular-nums で桁が揃う。
```
**違和感はたいてい 1〜3 軸のズレに収束する**(C の試し打ちで確認)。

### 5.3 ルーブリックは育てる(draft 運用)
パターンごとに 1 ファイル。実走のたびに新しい値候補を足す(語彙の育成)。
初期は table のみ。フィルタバー / カード / フォーム / 課金表 / ナビ … と実需に応じて増やす。
**`provenance: referenced`** を付け、各軸の根拠(一次ドキュメント or 観察)を明記。

---

## 6. 成果物スキーマと provenance クラス

### 6.1 置き場所(完全分離 / §2)
```
<workspace>/
└── web-design-scout/                  # ← mock/distill と別ベース
    ├── _rubrics/                       # 診断ルーブリック(パターン別・再利用)
    │   └── table.md
    └── <pattern>-shot/                 # 1 偵察 = 1 フォルダ
        ├── 00-scout-state.md           # 薄い baton(現フェーズ/枚数/next-action)
        ├── sources.md                  # 出典台帳(各カードの URL + 推定の度合い)
        ├── gallery.html                # 横並びギャラリー(出典+推定ラベル+診断)
        └── diagnosis.md                # (任意)現行モックとの差分が大きい時に分離
```
- `_rubrics/` は underscore 前頭で「shot ではない」signal(distill の `_philosophies/` に倣う)。

### 6.2 provenance — 第3クラス `referenced`
| クラス | 出自 | 値の保証 | スキル |
|---|---|---|---|
| `authoritative` | 公式デザインシステム | 公式トークン | mock |
| `empirical` | モック実測 + D4 検証 | 実測 + ラウンドトリップ | distill |
| **`referenced`** | **実プロダクト観察** | **作法は出典 / 値は推定** | **scout** |

`referenced` は 2 サブタイプ: `primary`(公式 docs・正確)/ `secondary`(実プロダクト・推定)。
**このクラスは scout 内に閉じる**(mock の provenance 体系に書き戻さない = §2 完全分離)。

---

## 7. 機械チェック(自前・軽量)

mock の `validate.mjs` は**借りない**(完全分離 / §2)。理由と差分:

- mock の不変条件「**accent 定義は 1 箇所**」は scout に**合わない** ― ギャラリーは複数パターンが
  各自のトークン名前空間(`--ln-*` / `--st-*` / `--nt-*`)で共存する。単一 accent を強制できない。
- scout の不変条件は別物。自前チェッカ `scripts/scout-lint.mjs`(B で実装)で見るのは:
  1. **各カードに出典リンクがあるか**(出典なしカード = 禁止)。
  2. **各カードに `推定 estimated` 表示があるか**(referenced-secondary の正直さ担保)。
  3. **ブランド固有名のベタ書きデータが無いか**(ダミーデータ置換の確認)。
  4. 自己完結 HTML か(外部 CSS フレームワーク不使用 ― mock と共有する唯一の出力規律)。

> mock=「トークン器の不変条件」、scout=「出典と正直さの不変条件」。チェックの**目的が違う**から
> 共有しない。これも責務分離の現れ。

---

## 8. mock / distill との接続 — 作らない(手渡しのみ)

distill は親ルーターに発見ステップを足した(接続を作った)。scout は**接続を作らないことが要件**:

- 採用カードの本体移植 = **ユーザーが手で**(範囲限定ハイブリッド §6 として mock 側で取り込む)。
- scout が mock を自動起動しない / mock が scout を発見しない。
- 唯一の論理的接点は人間の頭の中(「この罫線戦略を採る」と決める)。

> 「自動で繋がっていない」ことが欠陥ではなく**設計判断**(ユーザー要件)。将来繋ぎたくなったら、
> distill と同じく明示的な接続点を 1 つ足せばよい(その時は本書を改訂)。

---

## 9. C(試し打ち)で確定した学び

1. **推定ラベルは必須**(飾りでない): 一次/二次とも正確な px を出さないと実証(原則1)。
2. **観察軸ベース診断は成立する**: 罫線/整列/密度/状態表現は出典で裏取りできる(原則2)。
3. **二次でモック・一次で根拠**の役割分担が有効(原則3)。
4. **同一ダミーデータの描き分け**が比較として機能する(作法差が際立つ)。
5. 診断は **1〜3 軸のズレに収束**する(語彙テーブルが効く)。

---

## 10. TODO(次に詰める = フェーズ B)

- `.claude/skills/web-design-scout/SKILL.md` 本体(ルーター + S1–S5 + 出力規律)。
- `scripts/scout-lint.mjs`(§7 の自前チェッカ)。
- `scripts/list-shots.mjs`(再開発見。mock の `list-projects.mjs` を範に取る)。
- 偵察の並列化(S2)を Workflow で回すかの判断(ユーザー opt-in 前提)。
- ルーブリック追加: filter-bar / card / form / pricing-table / nav。
- WebFetch がブロックされる実プロダクト(ログイン必須/SPA)の劣化動作 ― 二次情報(解説記事/
  公式ブログ/Dribbble)に退避し出典を明示する手順を SKILL.md に明記。
- 著作権ガード文言(模写 + 出典 / アセット非転載 / ダミーデータ置換)を SKILL.md に明記。
