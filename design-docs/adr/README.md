# ADR（Architecture Decision Record）

本構造に至る設計判断を記録する。「なぜこの形なのか」を後から追え、再議論のコストを下げるため。

- 命名規則：`ADR-{NNN}-{タイトルをケバブケース}.md`（`naming_convention.md` §2）。
- 1判断1ファイル。決定の背景・選択肢・採否・理由を書く。

> 本PoCでは骨組みのみ。既存のFE関連ADRは `FE_architecture/ADR/` にある。設計書標準側の判断ADRは本フォルダに追加する。
