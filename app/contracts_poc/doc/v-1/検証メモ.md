## メモ
- Orval
  - FetchClient生成で使用想定
    - Zodの生成は使わない
    - FetchClientで生成させたコードは型アサーションを行っているが、今回contractsとしてBEの型安全を担保するので許容
  - ランタイムエラー
    - error.tsxを配置でエラー画面を表示させる
- バリデーション
  - 基本方針はバックエンドのバリデーションで担保、フロントでは最低限必要なバリデーションを行う想定
    - FEから来るリクエストボディ、クエリパラメータは一切信用すべきではない
- フォームバリデーション
  - zodのスキーマはFE層で再定義？
  - contractsから生成したFetchClientからzodスキーマを生成するか
    - tsの型を生成してそれを元に作るのが自然か
    - TODO：contractsからFE層、BE層がインターフェースを定義した際に、FE,BEの中でどの層がcontractsの情報にアクセスするのか、どこまでビルドで落ちるようにするのか具体として設計に落として置く必要がある
