# web-design-distill スキル 設計書

`web-design-mock` の**逆向き**スキル(モック → 再利用可能な哲学)の設計判断と根拠を残す。
運用には不要。実装の正は `.claude/skills/web-design-distill/SKILL.md`。本書は「なぜそう作ったか」。

> 対になる文書: `web-design設計.md`(親スキル = 哲学 → モック)。本書はその語彙・作法
> (二層ループ / フェーズ間ゲート / `00-state.md` baton / progressive disclosure / 値を捏造しない /
> 本物感の源)を**引き継ぐ前提**で書く。重複は再掲しない。

---

## 1. 目的

親スキル `web-design-mock` は「権威ある哲学(Apple/Carbon/Ant)→ モック」。
本スキルは逆に「**良いモック → 再利用可能な哲学**」を作る。狙いは**フィードバックループ**:

- 親で生まれた良い1枚に埋め込まれた暗黙のルールを、`references/<name>/<name>.md` と**同じ形**の
  明示的な哲学へ蒸留する。
- 蒸留した哲学を親のルーターに戻し、以後のモック生成で**選べる**ようにする(横展開の自動化)。

親 SKILL.md §8「横展開(哲学の追加)」は手作業前提だった。本スキルはそれを**検証付きの手順**にする。

---

## 2. 親スキルとの関係(共有契約)

- **スキーマ共有**: 成果物は `references/apple/apple.md` の §0〜§6 構造を厳守。別物を作らない。
- **機械チェック共有**: `validate.mjs`(器の不変条件)を D4 再生成物に流用。重複ベンダリングしない。
- **接続点**: 親 §5 内側パイプラインの「哲学を選ぶ」に empirical 哲学を足す = ループが閉じる。
- **独立スキルにした理由(形態の判断)**: 責務が逆向きで別ループ。親 SKILL.md を肥大化させない
  (親の設計価値)。単独で任意モックからも起動できる。→ `web-design-distill` として分離。

---

## 3. 核心の課題と 2 原則(設計の出発点)

Apple/Carbon が哲学として機能するのは**外部権威(公式デザインシステム)**が値を保証するから。
**1枚のモックから起こす哲学には外部権威が無い** → 放置すると「このHTMLがたまたまこうだった」
という**過学習スナップショット**に堕する。これが本スキル最大のリスク。全設計をここに収束させた。

### 原則1 — トークンは「実測」、原則は「推論」
- `:root` は計測可能な地の真実 → **逐語抽出のみ・捏造ゼロ**(親の「値を捏造しない」を継承)。
- §1原則・§4規約は解釈 → 必ず `inferred` タグ + 根拠を付け、**HTML の実使用と照合して反証可能**に。
  「言ったルールをモック自身が守っているか」を採用基準にする。

### 原則2 — 再利用性は「主張」でなく「検証」で得る
- 別画面で再生成して崩れなければ初めて哲学。これを最終ゲート(D4 ラウンドトリップ)にする。
- D4 を通らないものは哲学に昇格させない。

> この 2 原則が、本スキルが単なる「トークン吸い出しツール」と分かれる点。

---

## 4. 4フェーズのウォーターフォール

親と同じ作法(フェーズ間に検証ゲート / Claude は自己承認しない / ユーザーの役割は検証・批判)。

| | フェーズ | 出力 | 本質 |
|---|---|---|---|
| **D1** | 計測 (Capture) | `D1-measure.md` | `:root` 逐語抽出 + 実使用棚卸し + **親哲学とのデルタ**。**推論禁止**。各観測に出典(行/セレクタ) |
| **D2** | 原則の言語化 (Articulate) | `D2-principles.md` | コア原則 + 本物感の源を推論。**モック自身が守らないルールは原則にしない** + **非自明な原則を拾う** |
| **D3** | 草案 (Draft) | `_philosophies/<name>/<name>.md` | apple.md §0〜§6 踏襲。**独立 or 派生(extends)を選ぶ**。出所表で実測/推論を区分 |
| **D4** | 汎化検証 (Generalize) | `D4-validation.md` + 再生成HTML | **主ゲート**。別画面で再現するか検証して昇格 |

### 4.0 ドッグフーディングで見つけた3つの穴(2026-06-27, user-account-admin で実走)
最初の実例(Apple base + 濃紺サイドバー上書きの管理画面)を D1 まで走らせて、設計の不足が3つ出た。
いずれも反映済み:
1. **派生哲学型が無かった** → §6 に `kind: standalone | variant` + `extends` を追加(下記 6.2)。
   ソースの ~6割が Apple 逐語だったため、ゼロから組む独立哲学を強制すると冗長・不誠実だった。
2. **D1 にデルタ計測の指示が無かった** → ソースが base 哲学を宣言していれば「同値/上書き/追加/省略」の
   4区分で親リファレンスと差分を取る、を D1 に追加。最も情報量が多いのがこの差分。
3. **非自明な原則を取り逃す恐れ** → D2 に明記。実例では accent はフォーカスリングのみで、ブランドは
   濃紺 chrome ゾーンで表現(brand-via-chrome, not accent-fill)。既定の思い込みで計測を読まない。

### 4.1 D2 を「原則数固定」にしなかった理由(確定した判断)
当初「3つ前後」と書いたが、背骨が 1〜2 本で足りる哲学も 4〜5 本要る哲学もある。
「3つ揃えるため」に薄い原則を水増しすると、原則2(反証可能性)が弱る。
→ **原則の数は哲学が決める(可変)**。唯一の採用基準は数でなく「計測で裏付けられること」。

---

## 5. D4 の詳細設計(本書の核 — 一番議論したところ)

D4 が答える問いは1つ:**「この哲学は再利用可能か、1枚のスナップショットに過ぎないか」**。
当初の D4 は「崩れたら過学習 or ルーター狭める」と曖昧だった。原因は**2つの別物を1テストに
混ぜていた**こと。これを 2 軸に分解して機械的な分岐に落とした。

### 5.1 軸1 — どの画面で試すか
- **同型**(同じ画面タイプ・別コンテンツ): 測るのは**再現性**。「この種なら今後も同じ見た目が出る」
  という核心の主張。**必ず通す必要がある**。
- **異型**(別の画面タイプ): 測るのは**汎化の境界**。崩れるのは欠陥ではなく、§0 ルーターの
  得意/不得意を**発見する手段**(LP 由来の哲学が密ダッシュボードで崩れるのは正常)。

### 5.2 軸2 — 崩れ方(失敗モード)
- **不足(gap)**: 再生成に要るトークン/規約を哲学が供給できず、ジェネレータが**捏造**した
  → **哲学の穴=欠陥**。
- **ずれ(drift)**: 哲学は揃っていたが結果が**別物に見えた**。

### 5.3 失敗マトリクス(差し戻し先が機械的に決まる)

| | 不足(gap) | ずれ(drift) |
|---|---|---|
| **同型で発生** | 哲学の穴 → D3/D2 差し戻し | 過学習 → D2 で原則を直す |
| **異型で発生** | ※下記の判別が要る(5.3a) | スコープ外 → §0 に `unfit` 記録(正常) |

#### 5.3a 異型の「不足」は2種類(D4 実走 2026-06-27 で判明)
当初マトリクスは「異型 × 不足 = 哲学の穴 → 差し戻し」と単純化していたが、aux-admin を LP に転用する
異型テストで破綻が見えた。欠けたトークンが:
- (a) 哲学の**得意(in-scope)画面に要る** → 真の穴 → 差し戻し。
- (b) 哲学が**意図して省略**(§3.省)し、その異型ジャンルだけが要求 → **穴ではない。`unfit`**。
  例: admin 哲学が省略した largetitle を LP が要求。これを「穴」と扱い差し戻すと、admin 向けに削った
  哲学が LP 都合で太り、in-scope 画面を壊す。
- **運用規則**: 異型では**捏造より忠実遵守を優先**。足りなくても作らない → 結果は「ジャンル的に壊れた=
  ずれ」になり正しく `unfit` へ落ちる。捏造して成立させると (a) と誤判定する。

#### 5.3b 機械チェックは異型の unfit を検出できない(同上で判明)
哲学に忠実な異型は `validate.mjs` PASS / `anti-slop.mjs` CLEAN になりうる(捏造していないので機械的に
綺麗)。実際 LP は両方クリアしたが、盲検判定では weak(28)。**異型の境界判定はルーブリック項目6
(親子/ジャンル適合)=人間 or 盲検エージェントが決定打**。これは「項目6 を機械化しない/盲検第二意見を
任意で置く」という §5.6 の判断を、異型で強く裏づけた。

### 5.4 循環の罠への防御(D4 を「哲学の」テストにする条件)
`web-design-mock` は単体で有能(anti-slop / 良い既定値)。普通に再生成すると
「**ジェネレータが上手いだけ**」で哲学の穴が隠れ、D4 が無意味になる(循環論法)。よって鉄則:

> 再生成は**哲学ファイル 100% から導出**(Apple/Carbon と全く同じ経路)。**ソースモックの HTML は
> 見ない**(コピー防止)。捏造せざるを得なかったトークン/規約は**すべて「不足」としてログ**し、
> 黙って埋めさせない。

これで初めて「書かれた哲学だけで"らしさ"を運べるか」が分離できる。**本節が D4 の生命線**。

### 5.5 サブステップ
- **D4.1 選定**: 同型1枚(必須)+ 異型1枚(既定)。過学習を厚く炙るなら同型を別コンテンツで
  もう1枚、境界を広く取るなら異型を足す(増減自由)。→ **既定枚数の確定判断は §7**。
- **D4.2 再生成**: 哲学のみを入力に親 §5 パイプラインを回す。捏造は「不足」ログ。**親スキルの
  起動を伴う**ので、勝手に走らせずユーザーへ起動を促す。
- **D4.3 採点(ルーブリック)**: 再生成 vs ソースを**哲学レベルの属性**で比較(コンテンツ/ピクセル
  一致ではない)。各項目を **再現 / ずれ / 不足** で判定:
  1. トークン被覆(捏造ゼロ=合格)※半機械: `validate.mjs` + 哲学に無い `:root` 追加の有無
  2. 余白リズム(同じ space スケール / セクションリズムの系列か)
  3. タイポ階調(hero→body→caption の比率関係が同じか)
  4. 奥行きの作り方(shadow か 素材/層か、戦略が同じか)
  5. accent 規律(使われ方=数・配置の作法が同じか)
  6. 親子に見えるか(全体として「同じデザインシステム産」か)※ホリスティック
- **D4.4 確定**:
  - **昇格条件(ハードゲート)**: 同型で**不足ゼロ**。穴が1つでもあれば D3/D2 へ差し戻し。
  - **ずれの扱い**: 軽微なら可・本質的なら差し戻しを**ユーザー判断**(同型の本質的ずれ=過学習)。
  - **スコープ地図**: 各画面タイプを `validated / partial / unfit` で §0 メタに記録し続ける
    (二値にしない)。`validated_screens` には `validated` のみ。

### 5.6 採点者は誰か(確定した判断)
1〜5 番は半機械化の芽があるが、**6番(親子に見えるか)は原理的に人間 or LLM 判断**。
→ **採点はユーザーゲート**(親スキルの「ユーザーが検証・批判」をそのまま)。任意で、ソース/再生成を
見ていない**独立エージェントに盲検の第二意見**を取らせてよい(過学習の自己ごまかし対策)。
フル自動採点は将来課題(§8)。

---

## 6. 成果物スキーマと provenance クラス

`references/apple/apple.md` の §0〜§6 を厳守。差分は **front matter と §2 出所クラス**のみ。

### 6.1 独立(standalone)と派生(variant)— §4.0 穴1 の反映
D1 デルタ計測でトークンの大半が既存 authoritative 哲学と同値なら、それは独立哲学ではなく
**variant(派生)**。`extends: <base>` を宣言し §3 トークンは**デルタのみ**書く(同値は base 参照・
再掲しない)。理由は2つ: (a) 親スキルの「ポインタであって複製ではない」を継承、(b) base が更新された
とき variant が追従できる(全量複製すると陳腐化する)。最終モック生成・D4 再生成では親パイプラインが
`base の :root` ← `variant のデルタ` の順でマージする。

```yaml
---
name: <philosophy-name>
provenance: empirical            # authoritative(Apple/Carbon)と区別する新クラス
kind: standalone                 # standalone | variant
extends:                         # kind: variant のときだけ(base リファレンス名。例 apple)
derived_from: <slug>/output/mock-vN.html
validated_screens: []            # D4 で同型再現を実証できた画面タイプだけ(空=未実証)
screen_scope:                    # D4.4 スコープ地図(二値にしない)
  validated: []
  partial: []
  unfit: []
status: draft                    # draft | approved(D4 で同型・不足ゼロを満たすと approved)
updated_at: <date>
---
```

### 6.2 「権威の正直さ」(設計の倫理)
authoritative(外部公式)と empirical(実測由来)を**棚レベルで区別**する。親ルーターが empirical
哲学を提示するときは `provenance` と `validated_screens` を**表示してユーザーに舵を渡す**(出自を
隠さない)。§0 ルーター用メタの「得意な画面タイプ」は**D4 で実証できたものだけ**(主張の過大化を防ぐ)。

---

## 7. 置き場所・昇格先(確定した判断)

```
<workspace>/web-design-mock/
├── <slug>/
│   ├── output/mock-vN.html        # 蒸留のソース(地の真実)
│   └── distill/                    # 本スキルの作業状態(ソースと同居)
│       ├── 00-distill-state.md     # 薄い baton
│       ├── D1-measure.md / D2-principles.md / D4-validation.md
│       └── output/regen-<screen>.html
└── _philosophies/<name>/<name>.md  # 昇格した empirical 哲学
```

- **`_philosophies/` は underscore 前頭**でプロジェクト slug と区別(「プロジェクトではない」signal)。
- **スキル本体 `references/` には直書きしない**: authoritative の棚を empirical で汚さない / 配布物
  (`.claude/skills/...`)を汚さない(親 §4.0 の鉄則を継承)。代わりに親ルーターに「ユーザー哲学も
  発見する」ステップを足す(§8 接続課題)。

### 7.1 入力ソース(確定した判断)
MVP の入力は**親スキル群が生成した `output/mock-vN.html` のみ**(`:root` が既に揃い抽出が確実)。
任意 HTML / URL / 画像はスコープ外(信頼できる計測ツールが前提。caption でない実測が要る)。

### 7.2 検証枚数の既定(確定した判断)
D4.1 の既定は**同型1 + 異型1**(最小で両軸カバー)。増減自由。

---

## 8. 親スキルへの接続(✅ 実装済み — 親 SKILL.md §3.5 + §5)

蒸留哲学を親が選べるようにするのが最終目的。最小接続点:
1. 親 SKILL.md §3 ルーターに「**ユーザー哲学(`_philosophies/`, provenance: empirical)も発見・候補に
   含める**」一文 + 発見ステップ。
2. 親 §5「哲学を選ぶ」で empirical を許可。選択時に provenance / validated_screens を表示。
3. `validate.mjs` 共有のまま。`anti-slop.mjs` は D4 再生成物に任意適用。

> ここは**親への変更**を伴う。本スキル単体で完結しないので、昇格時にユーザーへ「親ルーターに発見
> ステップを足すか」を確認する(勝手に親を書き換えない)。具体文言は未確定 → §9。

---

## 9. TODO(次に詰める)

- ~~**実例で1本通す**~~ **完了**(2026-06-27, user-account-admin で D1→D4 実走 → `aux-admin` 昇格。
  D4.2「捏造=不足ログ」・D4.3 ルーブリックは実運用で機能。実走で見つけた穴は §4.0 / §5.3a / §5.3b に反映済み)。
- ~~`scripts/list-distills.mjs`(§1 発見スクリプト)~~ **完了**(親 `list-projects.mjs` を範に。`--hook` モード対応)。
- ~~親ルーターの「empirical 哲学 発見ステップ」具体文言(§8)~~ **完了**(親 SKILL.md §3.5 + §5 variant マージ。§8 のループは閉じた)。
- D1 任意ツール `extract-tokens.mjs`(`:root` 逐語抽出 + 実使用棚卸しの機械化)。
- D4.3 ルーブリック 1〜5 番の半機械化(6番は人間/LLM のまま)。
- 入力ソース拡張(任意 HTML / URL / 画像)= 計測信頼性が前提条件。
