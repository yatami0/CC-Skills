# web-design-mock スキル 設計書

Claude Code で動かす「HTMLのWebデザインモック生成スキル」の全体フロー設計。
本書は**構成の確定事項**をまとめたもの。各フェーズで呼ぶ具体的なskillの選定は別途(末尾TODO参照)。

---

## 1. 目的

毎回ゼロから「いい感じに」やらせると見た目も手順も揺れる。権威あるデザインシステム
(Apple HIG, IBM Carbon等)の原則とトークンを型に固め、一貫した手順で高品質なモックを
生成できるようにする。1セッションでできることは限られるため、**作業状況をログファイルに
残し、セッションをまたいで再開できる**ようにする。

---

## 2. 二層構造(混同しないこと)

このスキルは入れ子の2ループで動く。

- **外側ループ = 4フェーズのウォーターフォール**
  (要件定義 → 基本設計 → 詳細設計 → 実装)
  フェーズはゲートされる。現フェーズをユーザーが検証するまで次に進まない。
  複数セッションにまたがり、ログファイルで追跡する。
- **内側ループ = token→layout→HTML パイプライン**
  1枚のHTMLを生み出す。フェーズの**内側**(主に詳細設計・実装)で回る。高速・無償。
  v1→v7 のような素早い反復はここ。反復間にゲートは無い。

> ゲートは**フェーズ間にのみ**存在し、内側ループの反復間には存在しない。
> mock-v1→v7 を詳細設計フェーズ内で回すのは承認不要。基本設計へ**戻る**、
> 実装へ**進む**には承認が要る。

---

## 3. 確定した設計方針

### 3.1 親子構造(精度最優先)
デザインシステムは原則が互いに矛盾する(例: Apple=明瞭さ/控えめ/奥行きに基づく余白の効いた
構成 / Carbon=離散的な背景レイヤー(White↔Gray10)で面の階層を作る)。1ファイルに同居させると
平均化されて無個性になる。よって:

> 注: 哲学の対立軸を「影 vs 面」と単純化しないこと。Material も v3(Material You)で
> surface tint を非推奨化し、影より**トーンベースのサーフェス色**で階層を作る方向に移った。
> Material を扱う場合は必ず **M3 基準**(ダイナミックカラー/トーナルパレット + トーンベース
> サーフェス階層)で記述する。「エレベーションと鮮やかな色」は Material 2 の像なので使わない。
- **親 SKILL.md** = ルーター + 共通パイプライン + 出力ルール(「器の形」だけ共通化)
- **子 references/<philosophy>/<philosophy>.md** = 各哲学の原則・正確なトークン値・
  レイアウト規約(色/影/余白の実値は子に閉じ込める)

### 3.2 パイプライン(常にこの順)
方向性(人間の入力) → 哲学を選ぶ → リファレンスを読む → デザイントークン生成 →
レイアウト適用 → HTML出力。
- トークンは哲学から導出する(独立に作らない)
- HTMLはトークンを消費する(生の色/px値を直書きしない。全部 `var(--…)`)

> **トークンの出所は哲学ごとに異なる(重要)**。Carbon と Material 3 は機械可読な
> 公式トークンを正式配布している(Carbon: 8px 2x Grid・IBM Plex・layer/spacing/type
> トークン / M3: design-token + Theme Builder)ので「リファレンスの値をそのまま
> `:root` 化」できる。一方 **Apple は固定の数値トークン表を公開していない** —
> Dynamic Type のセマンティックスタイル + ごく少数の確定数値(タッチ最小 44pt、
> コントラスト 4.5:1 / 3:1)のみ。よって Apple 子リファレンスは「トークン表のコピー」
> ではなく **パターン + 確定 a11y 数値**から `:root` を組む。出力ルール(全て `var(--…)`)
> は哲学に依らず不変だが、**値の根拠の作り方が哲学ごとに違う**ことを子リファレンスに明記する。

### 3.3 哲学は「画面タイプ」で選ぶ(トーンは二次)
作る画面の種類によってふさわしい権威が違う。マーケLPならApple、エンタープライズ
管理画面ならCarbon。ルーターの選択軸は「画面タイプが第一、トーン/好みが第二」。

> 注: Apple HIG は本来 **アプリUI 向け**の指針であり、「マーケLP=Apple」は本スキルが
> 行う割当(著者マッピング)であって Apple 公式の用途宣言ではない。Carbon=エンタープライズ
> 管理画面は Carbon 公式のポジショニング通り。各哲学の「得意な画面タイプ」とその根拠は
> 子リファレンスに明記し、ルーターの選択がぶれないようにする。

### 3.4 範囲限定ハイブリッドはOK(無秩序な混在はNG)
哲学を丸ごと混ぜると無個性化するが、「ベースはApple哲学のまま、データUIの階層付けだけ
Carbon式を借りる」のようにゾーン/範囲を限定して借りるのは有効。借用値は専用の
名前空間トークン(例 `--sb-*`)に閉じ込め、本体の哲学トークンを一切壊さない。

### 3.5 ゲートは厳密
前フェーズが `approved` になるまで次フェーズの作業に進まない。承認はユーザーの明示的な
GOでのみ行い、Claudeが自己承認しない。ただし**内側ループの反復は自由**(3.2の二層構造)。

---

## 4. 4フェーズのウォーターフォール

各フェーズの進め方は共通: (a) 理解した仕様を2〜4行で言い換えて返す → (b) 方向を分ける
1〜3問だけ確認(詳細は詰めない) → (c) 作業しフェーズファイルに書く → (d) 進む前に
**検証ゲート**を持つ。ゲートでのユーザーの役割は「承認印を押す」ことではなく
**「検証・批判」すること**(人間の意図は初回で完璧に枠付けされない。ここで穴を捕まえる
ことが下流の手戻りを防ぐ)。`approved` への昇格はユーザーのみが行う。

### フェーズ1 — 要件定義
- 何のサイトか・誰向けか・トーン(人間からの入力。無ければ一度だけ簡潔に聞く)
- **哲学を画面タイプで選ぶ**(3.3)。ユーザーが指定したらそれを使う。2つを混ぜない。
- → `01-requirements.md` に記録
- **ゲート(検証観点)**: サイトの目的・対象を取り違えていないか? 選んだ哲学はこの
  「画面タイプ」に正しいか(単なる好みになっていないか)? 見落とした制約は
  (ロケール/CJK・アクセシビリティ・データ密度)?

### フェーズ2 — 基本設計
- 画面構成、レイアウトの骨格、機能インベントリ(ナビ/サイドバー/テーブル/カード/
  検索/ページネーション等)。何を載せ何を削るかの高速ループ。
- 哲学のレイアウト規約(グリッド・ブレークポイント・セクションリズム)を骨格レベルで適用。
  細かいスタイリングはまだ。
- → `02-basic-design.md` に記録
- **ゲート(検証観点)**: 必要な画面・要素が揃っているか? 載せるべきものが欠けて
  いないか・冗長なものを含んでいないか?

### フェーズ3 — 詳細設計
- **哲学リファレンスを読む**(記憶で作らない)。正確な余白/タイポ/影/色規律が本物感の源。
- **デザイントークン生成**: リファレンスの値から `:root` ブロックを出す。全スタイルの単一の真実。
  (トークンの出所は哲学ごとに異なる。Carbon/M3 は公式トークンをコピー、Apple はパターン+
  確定 a11y 数値から組む。詳細は 3.2 の注記を参照。)
- HTMLを高速反復(mock-v1→vN)。反復間にゲート無し。各修正と最終トークン決定を
  `03-detail-design.md` に記録(哲学は01を参照、再掲しない)。
- 範囲限定ハイブリッドは可(3.4)。借用値は名前空間トークンに閉じ込める。
- **ゲート(検証観点)**: 実装前に 01/02/03 を**読むだけ**の整合性チェック。詳細の
  選択が要件と矛盾していないか(例: 要件で「高齢者向け」と言ったのに本文が
  リファレンス最小サイズを下回った/ステータス色がアクセントと競合し始めた)。
  矛盾は実装に持ち込む前に直す(持ち込むとカスケードする)。

### フェーズ4 — 実装
- 最終成果物は `output/` 内の自己完結した単一 `.html` ファイル。これが納品物。
- 出力ルールを適用。機械チェック可能な不変条件を確認(ハードコード値ゼロ・accent定義
  1箇所・全参照 `var(--…)`)。
- `/mnt/user-data/outputs/` にも保存(ユーザーがダウンロードできるように)。
- **Figmaエクスポートは任意・スコープ外**。HTMLが納品物。Figma希望時は別の取り込み経路
  (FigmaのHTML import / プラグイン)が要る旨を伝え、後続作業として提示する。

---

## 5. ログファイル(セッション横断の真実の源)

プロジェクト状態はチャット履歴ではなくプロジェクトフォルダに置く。

### 置き場所の規約(確定)
スキルを `.claude/skills/web-design-mock/` に配布する構成になったため、置き場所を明確化:
- **ベース = 作業中ワークスペース直下の `web-design-mock/`**。各モックはその下の
  `<project-slug>/`(kebab-case。同名衝突は `-2`,`-3`)。
- **スキル本体ディレクトリ(`.claude/skills/...`)には書かない**。読み取り専用の配布物であり、
  状態/出力を置くと汚れて複数モックで衝突する。
- ユーザーが別パスを指定したらそれを優先。`web-design-mock/` の git 共有/除外は任意
  (スキルは `.gitignore` を勝手に書き換えない)。
- 起動時は `web-design-mock/*/00-state.md` を走査して再開/新規を選ばせる(SKILL.md §0)。

```
<workspace>/                  # Claude Code を起動したリポジトリ/フォルダ
└── web-design-mock/          # ベース(複数モックを束ねる)
    └── <project-slug>/       # 1モック = 1フォルダ
        ├── 00-state.md          # 薄いインデックス: 現フェーズ・ゲート状況・next-action・各ファイルへのポインタ
        ├── 01-requirements.md   # 何のサイト・どの哲学・どのジャンル
        ├── 02-basic-design.md   # 画面レイアウト・画面機能(載せる/削る)
        ├── 03-detail-design.md  # トークン決定 + デザイン修正ログ(v1→vN の履歴)
        └── output/
            └── mock-vN.html      # HTML成果物(バージョン管理)
```

### ログ運用ルール(権威あるhandoff/spec実践由来)

- **`00-state.md` は薄いbaton(アーカイブではない)**。書くのは「今どのフェーズ・各ゲートの
  状況・唯一のnext-action・どのファイルを開くか」だけ。どのセッションも最初にこれを読む。
  薄く保つことで新鮮なセッションが数秒で位置を把握でき、陳腐な詳細に引きずられない。
- **ポインタであって複製ではない**。フェーズファイルは互いを参照する(「哲学+トークンは
  01参照」)。複製すると肥大化し、陳腐化して矛盾し始める。
- **各フェーズファイルは front matter を持つ**: `status` / `updated_at` / `next-action`。
  次の一手の前に必ず `next-action` を読む。

### `00-state.md` テンプレート
```markdown
---
project: <name>
current_phase: requirements   # requirements | basic-design | detail-design | implementation
updated_at: <date>
next_action: <次セッションが最初にやる唯一のこと>
---
# State

| Phase | File | Status |
|---|---|---|
| 1. 要件定義   | 01-requirements.md   | draft        |
| 2. 基本設計   | 02-basic-design.md   | not-started  |
| 3. 詳細設計   | 03-detail-design.md  | not-started  |
| 4. 実装       | output/              | not-started  |

Status: not-started | draft | in-review | approved
毎セッション 00-state.md を最初に開く。次に current_phase のファイルを開き next_action を実行。
```

### フェーズファイル front matter テンプレート
```markdown
---
phase: requirements
status: draft        # draft | in-review | approved
updated_at: <date>
next_action: <具体的な次の一手>
---
```

---

## 6. 出力ルール(哲学に依らず適用)

- 自己完結した単一 `.html`: トークンは `<style>` ブロック内。外部CSSフレームワークは
  リファレンスが指定しない限り使わない。システムフォントスタックは可。有料フォントは引かない。
- `:root` トークンブロックがCSSの先頭。以降は全て `var(--…)` 参照。マークアップに
  マジックナンバーを撒かない。(機械チェック可能: ハードコード値ゼロ・accent定義1箇所・
  全参照 var()。tmpモックで通過済み。)
- モバイルファースト・レスポンシブ: ブレークポイントはリファレンスが定義。
- デフォルトでアクセシブル: リファレンスのコントラスト/タッチターゲット最小値を満たす。
- モックなので、本物らしいプレースホルダのコピーとレイアウトを使う(lorem ipsumの壁や
  実際の専有コンテンツは使わない)。
- 保存後、適用した哲学と look を決める数個のトークン決定を簡潔に伝える(ユーザーが舵を取れるように)。

---

## 6.5 SKILL.md オーサリング規約(Anthropic 公式準拠)

このスキル自体が Agent Skill なので、公式のオーサリング規約に従う。

- **progressive disclosure**: 親 `SKILL.md` 本文は **500行以内・コア手順のみ**。詳細・トークン値・
  レイアウト規約は子 `references/<philosophy>/` に置きオンデマンドで読む。references は SKILL.md
  から**1階層**に保つ(ネストの深掘りは部分読みの原因)。検証など固定処理は `scripts/` に置く
  (実行されるだけでコードは文脈に載らない)。§3.1 の親子構造はこの公式パターンそのもの。
- **frontmatter**: `name` は英小文字・数字・ハイフン、64字以内、gerund 形推奨。"claude"/"anthropic"
  を含められない。`description` は第三者視点(「〜する」)で **「何をする」と「いつ使う」の両方**を
  含め、やや *pushy* に書く(Claude はスキルをアンダートリガーしがち、と公式が警告)。
- **スキルはスキルを直接呼ばない**: フェーズ進行は「skill を呼ぶ」のではなく、Claude が
  オーケストレータとして **子リファレンスを読む / スクリプトを実行する**ことで実現する
  (TODO #1 参照)。
- **不変条件は scripts/ で機械検証**: §6 のチェック(ハードコード値ゼロ・accent定義1箇所・
  全参照 `var(--…)`)は `scripts/validate.*` として置き再現性を持たせる。「tmpモックで通過済み」の
  検証実体をここに固定する。

---

## 7. 横展開(哲学の追加)

各哲学は `references/<name>/` 配下の自己完結フォルダ。`<name>.md`(原則 + 正確なトークン値 +
レイアウト規約)と任意の `assets/`。追加時はAppleファイルの構造を踏襲し、各哲学固有の値を
自分のファイル内に閉じ込めて互いに漏れないようにする。
次の候補: **Carbon**(エンタープライズ管理画面の権威)。要点は「データ画面の階層は影ではなく
**離散的な背景レイヤートークン**(light テーマで White↔Gray10 を交互、`$layer-01/02/03`)で
作る」。8px の 2x Grid・IBM Plex・spacing/type/color トークンを公式配布しているので、値は
そのまま `:root` 化できる。(Carbon も modal/popover 等の真に浮く層には影を定義するので
「影を一切使わない」とは書かないこと — *面の階層*を色で作るのが要点。)

> Material(M3) を追加する場合は §3.1 の注記に従い、必ず M3 基準で書く
> (ダイナミックカラー/トーナルパレット + トーンベースサーフェス階層)。

---

## 8. Anti-AIっぽさ(AI slop 対策) ※設計方針・未確定

> 根拠の全文は `web-design-AIっぽさ調査レポート.md`(deep-research 調査)を参照。本章はその設計への落とし込み。
> ここは**方針の確定前**。最終決定は TODO(§9)で詰める。

### 8.1 なぜ要るか
整合化された LLM は意図的なデザイン選択をせず、学習データの統計的"平均値"に収束する(査読論文の
**mode collapse** / 選好データの **typicality bias**)。結果、出力が互いに似て既定の見た目に寄り、
人が一目で「AIだ」と見抜く。本スキルは哲学からトークンを導出する分**素の generic 生成よりは強い**が、
哲学の内側でも Inter・淡いグラデ・一律スペーシング・3アイコンボックスへドリフトしうるので、明示的な
対策を上乗せする。

> ★最重要の含意: **「トークンを渡すだけ」では AI 臭は消えない**(調査で 0-3 棄却)。
> トークン導出(§3.2)は**必要条件にすぎない**。「既定の名指し禁止 + 次元別誘導 + インスピレーション
> 参照」を上乗せして初めて効く(サンプリング分布をずらして mode collapse に対抗するため)。

### 8.2 二段構え(予防 + 検出)
研究が支持する構成を、本スキルの既存アーキテクチャに対応づける。

**前段 = 予防(ルールをプロンプト/リファレンスに組み込む)** — 親子構造(§3.1)にそのまま割り付く:
- **哲学非依存の禁止リスト → 親 SKILL.md(§7 出力ルール)**。例:
  - フォント: Inter / Roboto / Arial / Open Sans / Lato / 既定 system フォントに**逃げない**
  - 色: 白背景の紫(indigo/violet)グラデに**逃げない**。`bg-indigo-500` 的な既定アクセントを避ける
  - レイアウト: 「中央ヒーロー + アイコン付き3カラムカード」テンプレに**逃げない**
  - 階層: border-radius / padding を**一律にしない**(意図した階層を持たせる)
- **哲学固有の特徴付け → 子リファレンス**。例: 推奨フォント(哲学に合う具体名)、支配色+鋭い
  アクセントの作り方。Apple/Carbon は元々固有のフォント(SF/IBM Plex)と色規律を持つので、
  「既定の Inter/紫」へのドリフトは子リファレンスを**読ませる**ことで大半が防げる。
- これは§3.1(平均化を避ける親子分離)の思想と一致。**普遍的な禁止は親、特徴的な処方は子**。

**後段 = 検出(生成後に AI チェックを回す)** — 既存の機械検証(validate.mjs)と同じ思想だが、
**自動ゲートではなくユーザー起動で回す**(トリガー設計は §8.3):
- **(A) 決定論的 anti-slop lint(backbone)**: `scripts/validate.mjs` の発想を拡張。本スキルは色を
  すべて `:root` トークンに集約済み(§3.2)なので、**`:root` の値だけ**を見れば判定が容易:
  - accent の色相が indigo/violet レンジか / font-family が禁止フォントか / radius・spacing
    トークンが単一値に潰れていないか、等を機械チェック。Krebs 式(Playwright で 1,590 LP を
    決定論採点)の最小版。フリー・再現可能・文脈を食わない。
  - 落とし穴: tell は時間で変わる / 人間製ミニマルサイトを誤検出しうる(Krebs も誤検出 5-10%)。
    **ハードな gate でなく warning** にして人間ゲート(§3.5)で判断、が無難。
- **(B) 任意の LLM ルーブリック評価(second pass)**: 「AIっぽい?」と曖昧に訊くのではなく、
  §2 の tell チェックリストを**明示的に渡して**減点法で評価させ、次反復への具体フィードバックを得る。
  - 落とし穴: **自己採点バイアス**。生成と同じモデル/同じ mode collapse が判定すると、自分の
    平均的出力を「問題なし」と追認しがち。敵対的に(「tell を探せ」)+ スクリーンショット添付 +
    明示ルーブリック、で緩和する。コストと文脈を食う点も含め「任意」に留める。

### 8.3 どこに効かせるか(予防=通常フロー内 / 検出=ユーザー起動の追い込み)

**前段=予防は、新しい別工程を足さず通常フローの中で効かせる**(= フローを回す中で「既に
聞いている/適用している」状態にする):
- 哲学固有の処方(フォント/色)は子リファレンスを読む手順(§フェーズ3)で既に効く。
- 「複数デザイン方向を出す」(§8.4)は詳細設計の初回ステップとして通常フローに組み込む。
- 普遍的な禁止(逃げフォント/紫グラデ/3ボックス/一律 radius)は親 SKILL.md の出力ルールに
  規約として書き、生成時に効かせる。

**後段=検出(anti-slop)は、自動ゲートにせず「ユーザー起動の追い込みパス」にする**:
- 実装フェーズで自動実行する設計は**採らない**。代わりに、出てきた成果物(mock-vN)を
  ユーザーが確認 → **ユーザーの明示指示で anti-slop を回す** on-demand 動作にする。
- anti-slop を回すと `scripts/anti-slop.mjs`(advisory)で tell を検出 → その指摘を反映した
  **de-slop 版を別名で保存**する: `output/mock-vN-anti-slop.html`。
  - 内側ループ(§1, mock-v1→vN, ゲート無し)の**一反復**として扱う(承認不要)。
  - 別名サフィックス `-anti-slop` で「どの版に対する追い込みか」を辿れるようにする
    (単なる v(N+1) にせずトレーサビリティを持たせる)。
- 任意で LLM ルーブリック評価(§8.5 B)も同じ on-demand 動作の中で併用できる。

> この trigger 設計が良い理由: (1)人間ゲート(§3.5)とユーザー権限を尊重(勝手に走らせない・
> ブロックしない)、(2)既存の二層ループ(§1)に自然に乗る(anti-slop も mock-vN→vN' の一反復)、
> (3)warning 止めの決定(§8.4)と整合(そもそも明示指示でしか走らない)。

### 8.4 確定した方針
- **検出方式 = 決定論 lint を軸 + LLM ルーブリック評価は任意**(8.2 後段(A)を backbone、(B)は
  オプション)。`:root` 集約済みでトークン値検査が容易、フリー・再現可能・文脈を食わないため。
- **判定強度 = warning(ハード gate にしない)**。誤検出 5-10% を考慮し、lint は警告を出すだけで
  ブロックせず、既存の人間検証ゲート(§3.5)で最終判断する。自動リジェクトループの手戻り/
  「平均的選択へのすり替え」リスクを避ける。
- **複数デザイン方向 = 詳細設計フェーズの初回に複数案を出す**。mock-v1 を出す際に 2〜3 の
  異なる方向(色/タイポ/レイアウトの分岐)を提示しユーザーに選ばせる(調査§4-8。mode collapse
  対策として最も効く手の一つ)。選択後は通常どおり単一案を v2→vN で磨く(内側ループのゲート無しは維持)。
  - 影響: §5 パイプライン/フェーズ3(§フェーズ3)に「初回は複数方向」を組み込む。反復コスト増は
    初回のみに限定する(2回目以降は分岐しない)。

### 8.5 検出の詳細設計(確定)

**核心: ブラックリストでなく「自己トークン整合(self-conformance)」で見る。**
両哲学(Apple/Carbon)のトークンを確認した結果、AIっぽさの典型 tell は**既にトークンレベルで
構造的に排除されている**:

| tell | Apple のトークン | Carbon のトークン |
|---|---|---|
| Inter フォント | `--font-sans` = SF Pro スタック | = IBM Plex |
| indigo/紫アクセント | `--color-accent`=#007AFF (hue≈211 **青**) | =#0f62fe (hue≈219 **青**) |
| 一律 radius | radius-s/m/l/xl/pill の**スケール** | 既定 0(直角) |
| 淡い影で階層 | 影でなく素材(blur)/層 | 影でなく背景レイヤー段差 |

→ 「子リファレンスを忠実に `:root` にコピーすれば tell は出ない」。AIっぽさが出るのは
**モデルがリファレンスを無視して `:root` を汚すか、`:root` の外でハードコードした時**。よって
lint は **per-哲学の正解値をハードコードせず**(リファレンスとの二重管理ドリフトを避ける)、
**生成物が自分で宣言したトークンスケールから逸脱したか**を見る。既存 `validate.mjs` の
「色は全て var() 参照」と**同じ原理**をフォント/radius に拡張するだけ。

**実装方針: `validate.mjs`(ハード gate, exit 1)はそのまま。anti-slop は別スクリプト
`scripts/anti-slop.mjs`(advisory, 常に exit 0・warning のみ・ブロックしない)に分離。**
哲学を引数で渡さなくても動く(自己整合のため)。

| ID | チェック | 検出方法 | 信頼度 | 判定 |
|---|---|---|---|---|
| **T1 フォント** | Inter/Roboto/Arial/Open Sans/Lato に逃げてないか | `:root` の `--font-sans` 値の先頭語 + markup 内のハードコード `font-family` | 高 | warn |
| **T2 アクセント色相** | indigo/violet レンジか | `--color-accent` を HSL 化し **hue∈[230,300] かつ sat>0.5**(systemBlue 211 / Carbon 219 は安全圏) | 中 | soft warn(ブランド色の可能性) |
| **T3 グラデ** | 安易なグラデ多用/紫青グラデ | `*-gradient()` の出現数、stop 色が紫レンジに入るか | 中 | warn |
| **T4 radius 平坦化** | 階層が一律に潰れてないか | markup が参照する `var(--radius-*)` の distinct 数が実質 1 なら警告 | 高 | warn |

> **影整合は決定論 lint に持たせない(自己整合を保つ)**: 当初 T5(Carbon=タイルに影なし)を検討したが、
> 影の良し悪しは**哲学固有**(Carbon=影なし / Ant=白カードに微細影あり)で script に哲学知識を
> 持たせると自己整合の原則が崩れる。よって T5 は採用せず、影の整合は**子リファレンスの規律**
> または任意の **LLM ルーブリック評価**に委ねる。決定論 lint は T1–T4(哲学非依存)に限定する。

> hue 閾値の判断: Carbon の青(219)と indigo(239)の差が約 20°と薄い。**230** を境にすれば両者を
> 分離できるが際どいので、T2 は **warning 止まり**にする(gate にしない決定と整合)。

**LLM ルーブリック評価(B / 任意・second pass)**: 決定論で**見えないものだけ**に絞る —
平均化コピー("Build the future" 系)/ 3アイコンボックスのゲシュタルト / 全体の"安さ"。
自己採点バイアス対策として **スクショ添付 + §調査レポート§2 のチェックリスト明示渡し +
「tell を探せ」の敵対的フレーミング**を必須にする。コスト/文脈を食うため既定オフ・任意発火。

### 8.6 まだ詰める詳細(TODO §9)
- T1 の font-family 照合リストの最終確定(現案: inter/roboto/arial/open sans/lato + 既定 system のみ)。
- LLM ルーブリック評価(B)を実装するか/その発火条件(8.5 の必須要件を満たす形で)。
- eval への反映: 「AIっぽくないか/複数方向が出るか」を測る eval ケースを追加するか(現 evals は哲学選択/生成可否のみ)。

---

## 9. TODO(次に詰める)

- [x] **Anti-AIっぽさの実装(§8 方針確定済み)**:
  - [x] 前段=予防: 親 SKILL.md §7 に「AI slop を避ける規約」(逃げフォント/白背景紫グラデ/
        3アイコンボックス/一律 radius・padding/平均化コピー)を追記。子リファレンス(apple/carbon)は
        既に固有フォント・固有アクセントを処方済みのため**特徴付けの追記は不要**(重複回避)
  - [x] 後段=検出スクリプト: `scripts/anti-slop.mjs` を実装(自己トークン整合 / T1–T4 / 常に
        warning・exit 0)。clean/slop/carbon フィクスチャで検証済み(設計 §8.5)
  - [x] 後段=検出の配線: SKILL.md §フェーズ3 に**ユーザー起動の anti-slop 追い込みパス**
        (`mock-vN-anti-slop`)、§7 に `validate.mjs` との役割差を明記(§8.3)
  - [x] フェーズ3に「初回は複数デザイン方向(2〜3案)を出す」を組み込む(SKILL.md §フェーズ3/§5)
  - [x] eval ケース追加: `evals/04-anti-slop.json`(複数方向 / 既定フォント・紫グラデへの非ドリフト / anti-slop パス)
- [x] **各フェーズで読むリファレンス / 実行するスクリプトの定義**(「skill を呼ぶ」ではない。
      スキルはスキルを直接呼ばず Claude がオーケストレータ。§6.5 参照)。→ `SKILL.md §2` に
      各フェーズの読む ref / 書き出すファイル / 実行スクリプトを定義済み
  - フェーズ1(要件定義): 読む ref / 書き出す `01-requirements.md` のテンプレ
  - フェーズ2(基本設計): 読む ref / 書き出す `02-basic-design.md` のテンプレ
  - フェーズ3(詳細設計): 読む `references/<philosophy>/<philosophy>.md` / トークン生成手順
  - フェーズ4(実装): 実行する `scripts/validate.mjs`(不変条件チェック) / 出力先規約
- [x] **eval を先に3件 JSON で作る**(公式の最重要プラクティス: ドキュメントより先に評価)。
      → `evals/01-apple-lp.json` / `02-router-carbon.json` / `03-fallback.json` を作成済み:
  - ① Apple風LP が生成できるか
  - ② 哲学未指定の管理画面でルーターが画面タイプから Carbon を選べるか
  - ③ 未実装哲学を指定された時のフォールバックが効くか
  - ※ 未実施: skill 有無のベースライン比較 / Haiku・Sonnet・Opus での実行(実走はこれから)
- [x] 親 `SKILL.md` の作成 + frontmatter 確定(`name: web-design-mock`/`description` は「何を+いつ」+
      日英トリガー語, §6.5)。ルーター・パイプライン・ゲート・出力ルールを収録
- [x] 最初の子リファレンス `references/apple/apple.md` の作成(以降の哲学はこの構造を踏襲)。
      Apple は数値トークン非公開のため「パターン + 確定 a11y 数値(44pt, 4.5:1/3:1)」で組む(§3.2)
- [x] 2番目の子リファレンス `references/carbon/carbon.md` の作成(apple.md と同じ構造。公式トークンをコピー可)
- [x] `scripts/validate.mjs` の実装(ハードコード値ゼロ・accent定義1箇所・全参照 `var(--…)`)。
      Node・依存ゼロ。pass/fail フィクスチャで動作確認済み(Python 未インストール環境のため Node 採用)
- [x] **Claude Code スキルとして配置**(プロジェクトスキル形態)。正本を
      `.claude/skills/web-design-mock/` に再配置し `/web-design-mock` で自動検出。検証コマンドは
      CWD 非依存の `${CLAUDE_SKILL_DIR}` 経由に変更
- [x] **配布手段**: `install.sh` / `install.ps1`(非破壊。clone → 対象リポジトリ or `--user` に設置)。
      動作確認済み。将来は無改造で plugin 化へ移行可能(README 参照)
- [x] **README.md** 作成(使い方・設置・配布形態・哲学追加手順)
- [x] **ログファイルの置き場所の確定**: ベース=ワークスペース直下 `web-design-mock/<slug>/`。
      スキル本体には書かない。複数モックを束ね、起動時に走査して再開/新規を選ぶ
      (SKILL.md §0 / §4.0、設計書 §5 に反映)
- [x] **フェーズ進行の再開トリガー設計**: 2段構成で確定。
      ① 確実な核 = SKILL.md §0 で発見スクリプト `scripts/list-projects.mjs` を最初に実行し、
         `web-design-mock/*/00-state.md` を走査して再開/新規を選ばせる(決定的・同梱・テスト済み)。
      ② 任意の自動化 = 利用側リポの `.claude/settings.json` に SessionStart hook を追加
         (`list-projects.mjs --hook`。進行中モックがある時だけ context 注入)。
      ※ スキルに hook を同梱しない設計判断: Claude Code に「スキル起動」イベントは無く、
        スキル同梱の SessionStart hook は起動時点で発火しない(公式仕様で確認済み)。
        グローバル発火する SessionStart はスキルに同梱せず利用側のオプトインに留める。