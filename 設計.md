# FE 基盤構築 設計書

>本書は、マスタ機能の実装に入る前に整える「基盤（土台）」の設計を定める。対象は、アーキテクチャの決定に依存せず先に固められる部分に限る。基盤構築に必要な内容は本書だけで完結するよう記述する。
>
>本書中の Status 表記は次の意味で用いる。確定（決定済み）／一部確定（方針は決定、詳細は後決め）／仮置き（暫定。確定後に見直す）／未決（本書では決めない）。

---

## 0. 概要

対象は基幹システムのマスタデータ管理サイトで、フロント専任 1 名と AI コーディングエージェントが主体で開発する。判断ポイントを規約と機械ゲートで固定し、マスタ機能の実装をいつでも開始できる土台を先に通す。

### Goals（本書が対象とするもの）

確定済みで、マスタ機能の詳細が未確定でも先に作れるものを対象とする。

-モノレポ基盤（pnpm workspace。Turborepo は当面不採用＝§3.1）。`apps/`と`packages/`の骨格、root スクリプト（`pnpm -r`/`--filter`へ委譲）、`pnpm-workspace.yaml`
-共有設定 package（`packages/config`）。ESLint flat config、tsconfig base、Tailwind preset を集約する
-Next.js アプリ雛形（`apps/master`。App Router、TypeScript、Tailwind、クライアントファースト）
-TanStack Query Provider と QueryClient ファクトリ。データ取得は prefetch → hydrate を全画面の標準とする（§3.3 / §3.3.1）。`staleTime>0`既定と`page.tsx`薄テンプレを基盤で固定する
-BFF キャッチオール proxy の骨格（`apps/master/src/app/api/[...path]/route.ts`）
-OpenAPI codegen パイプライン（`apps/master/src/generated/`と drift 検出）
-機械ゲート（ESLint、tsc strict、codegen drift、Vitest、Playwright）を束ねる CI
-テストハーネス（Vitest + RTL + jsdom、Storybook、Playwright 設定）。基盤時点のテスト中身は最小スモークのみ
-規約・AI ルールの置き場（`frontend/`の`AGENTS.md`、`CLAUDE.md`、`.claude/`、`docs/conventions/`）
-API 契約スキーマの参照配線。契約本体は specs 側に置き、各サイトが対応サービスのものを参照して codegen する
-デプロイ前提（`output: 'standalone'`、Dockerfile、`pnpm --filter <app> deploy`）

### Non-Goals（本書が対象としないもの）

マスタ機能は未着手のため、次は意図的に除外する。土台だけを先に通す。

-`apps/master/src/features/master/`の画面・hooks・型の実装
-マスタのメタデータ／設定駆動 UI の採否（レビュー指摘 A）
-ファイル入出力（CSV アップロード／ダウンロード）の具体設計（レビュー指摘 B）
-ログイン画面・認可表示制御の具体実装（レビュー指摘 C。人間主導）
-楽観的更新、定義駆動 UI（基盤では prefetch → hydrate の配線・`staleTime>0`既定・テンプレ確立までを通す）
-master 以外の frontend アプリ（`apps/txdb`など）の追加。骨格だけ将来対応可能にしておく

>A・B・C（メタデータ駆動 / ファイル入出力 / 認可表示制御）は基盤の判断には不要で、本基盤の完了を妨げない。マスタ機能の着手前に別途合意する。

---

## 1. 検討事項

基盤に関わる検討事項と結論を一覧する。§3 以降の設計はこの結論を前提とする。Status の意味は冒頭の凡例を参照する。

| 検討事項 | 結論 | Status |
| --- | --- | --- |
| リポジトリ構成 | frontend / backend / specs を束ねる polyglot モノレポ。frontend の pnpm workspace root は`<repo-root>/frontend`（git root とは別）。現状は`apps/master`1 つで、将来マルチを前提とした骨格にする | 仮置き |
| パッケージマネージャ | pnpm（workspace 前提）。npm / yarn は不採用 | 確定 |
| モノレポツール | Turborepo は当面不採用。タスク実行は pnpm の `--filter`＋ `pnpm -r`（トポロジカル順）で足りる。再検討トリガーを満たしたら後付けする（§3.1）。Nx も不採用 | 確定 |
| データ取得 | prefetch → hydrate を全画面の標準とする。`page.tsx`（RSC）で prefetch、Client で`useQuery`、queryOptions を生成物で共有。グローバル`staleTime>0`既定（§3.3 / §3.3.1） | 確定 |
| lint / tsconfig / Tailwind の共有 | 共有 config package（`@repo/typescript-config`/`@repo/eslint-config`/ `@repo/tailwind-config`）に集約し、各 app が extends・import する。root に tsconfig は置かない | 確定 |
| API クライアント生成 | orval に一本化し、OpenAPI 1 本から型・client・TanStack Query hooks・MSW を生成する。Zod は手書き。旧 3 ツール分業は不採用（理由は §1.1） | 確定 |
| ケース変換（snake ⇄ camel） | ドメイン型は camelCase。境界変換は BFF で寄せる方針とし、詳細は実スキーマ確定時に決める（§4.4） | 一部確定 |
| server 枝（RSC prefetch）の認証経路 | A 案（BFF 経由で incoming cookie を転送）で確定。server 枝も BFF を必ず通し、トークン付与・セッション解決は BFF 内に閉じる。B 案（backend 直叩き＋トークン）は Next 側でトークンを扱うことになり「BFF が唯一のトークン付与点・ブラウザ／Next にトークンを出さない」方針に反するため却下（§3.5 / §4.5）。認証の中身（cookie 仕様・セッションストア）は認可設計（指摘 C・後続）で確定 | 一部確定 |
| デプロイ先（EKS / コンテナ） | `output: 'standalone'`を最初から有効化し、Dockerfile と`pnpm --filter <app> deploy`を基盤に含める（§4.2） | 確定 |

### 1.1 API クライアント生成ツールの選定

旧案は openapi-typescript（型）・openapi-fetch（client）・openapi-zod-client（Zod）の 3 ツール分業だった。これを orval 一本化へ置き換えた理由は次のとおり。

-openapi-fetch がメンテナンスモードへ移行した（deprecated ではなく「新機能停止・現状維持」。fetch ラッパの後継は示されず、公式は vendoring か TanStack Query 直利用を案内。開発リソースは openapi-typescript 本体に集約される）。
-旧案では MSW handlers を生成できず、モックを別途用意する必要があった（§3.6 の欠落）。
-orval なら型・client・TanStack Query hooks・MSW を 1 本のスキーマから生成でき、custom mutator で BFF 経由を構造的に強制できる（§3.5、§3.4）。

Zod だけは orval 生成を使わず手書きとした。生成 Zod は冗長になりやすく、入力バリデーションの意図を型から起こすほうが乖離を防げるためである。

---

## 2. ディレクトリ構成

リポジトリは frontend / backend / specs を束ねる polyglot モノレポで、この切り方は仮置きである。git リポジトリ root と pnpm workspace root は一致せず、pnpm workspace の root は`<repo-root>/frontend`に置く。本書が扱うのは `frontend/`サブツリーで、`apps/master`内の`src/`レイアウトは §2.2 に示す。

トップレベル名は仮置きで、旧 `FE`/ `BE`/ `Document`を `frontend`/ `backend`/ `specs`に改称している。`docs`ではなく `specs`としたのは、API 契約（OpenAPI）という真実の源を含むためである。なお他ドキュメントで使う略語「FE」「BE」は frontend / backend という概念を指し、ディレクトリ名ではない（改名対象外）。

### 2.1 リポジトリ全体

```
<repo-root>/                  # git リポジトリ root。frontend/backend/specs を束ねる
├─ frontend/                  # 本書の対象。pnpm workspace の root（§2.2）
├─ backend/                   # Java（Gradle/Maven）。本書対象外
└─ specs/                     # 仕様・ドキュメント。各サービス仕様が API 定義を内包
   └─ <service>/              #   サービス（≒サイト）ごとの仕様
      └─ API定義/
         └─ openapi.yaml      # API 契約。frontend は対応サービスのこれを参照して codegen（§3.5）
```

-言語ごとのツールバージョンは `frontend/mise.toml`（node）と `backend/mise.toml`（java）で各サブツリーが宣言し、`cd`で自動切替する。
-repo-root に横断ファイル（AGENTS.md / CLAUDE.md /`.github`（CI）/ 横断規約）をどう持つかは未決（§4.1 / §4.3）。本書では`frontend/`サブツリー内の配置のみ確定させる。

### 2.2 frontend サブツリー（`<repo-root>/frontend/`）

pnpm workspace の標準的な apps/packages 構成に合わせる（create-turbo の標準レイアウトとも一致するので、後で Turborepo を後付けしても構成は不変＝§3.1）。frontend root に`tsconfig.json`は置かず、共有 config を各 package が extends する。

```
frontend/                       # pnpm workspace root（git root とは別）
├─ package.json                 # "private": true。スクリプトは pnpm -r / --filter へ委譲のみ
├─ pnpm-workspace.yaml          # apps/* packages/* を workspace に登録
├─ pnpm-lock.yaml
├─ mise.toml                    # node バージョン固定
├─ AGENTS.md                    # frontend サブツリーの規約
├─ CLAUDE.md                    # 1 行目 @AGENTS.md で import
├─ CONTRIBUTING.md              # 人間向け開発手順・PR ルール
├─ .claude/                     # Claude Code 資産（AI 関連ルール）
│  ├─ skills/<name>/SKILL.md
│  ├─ commands/*.md
│  ├─ agents/*.md
│  ├─ settings.json             #   共有設定（権限・hooks・model）
│  └─ settings.local.json       #   個人設定（.gitignore）
├─ docs/
│  └─ conventions/              # 補足の散文のみ（規約本体は config package 側）
├─ packages/                    # 共有ライブラリ・設定（package 名は @repo/*）
│  ├─ typescript-config/        # base.json / nextjs.json を export
│  ├─ eslint-config/            # flat config（機械ゲート §3.7 を内包）
│  ├─ tailwind-config/          # preset
│  ├─ ui/                       # 横断 UI 部品（基盤時点は空〜最小・.gitkeep）
│  └─ lib/                      # 横断ユーティリティ（error / i18n）骨格
│
└─ apps/                        # アプリ（現状 master のみ。将来 txdb 等を追加）
   └─ master/
      ├─ package.json
      ├─ tsconfig.json          # extends @repo/typescript-config/nextjs.json
      ├─ eslint.config.js       # import @repo/eslint-config
      ├─ next.config.ts         # output: 'standalone' + outputFileTracingRoot（§4.2）
      ├─ orval.config.ts        # 入力は specs 内の対応サービスの openapi.yaml（§3.5）
      ├─ Dockerfile / .dockerignore
      ├─ scripts/               # gen:api 等
      ├─ tests/e2e/             # Playwright
      └─ src/
         ├─ app/
         │  ├─ layout.tsx       # providers を呼ぶだけ（Server のまま）
         │  ├─ providers.tsx    # 'use client'。QueryClientProvider（§3.3）
         │  ├─ page.tsx         # 疎通確認用の最小ページ（後で置換）
         │  └─ api/
         │     ├─ [...path]/route.ts  # BFF キャッチオール proxy（骨格・§3.4）
         │     └─ health/route.ts     # 疎通・監視用ヘルスチェック
         ├─ features/           # 空（マスタ未着手）。.gitkeep のみ
         ├─ components/ui/      # アプリ固有 UI
         ├─ lib/
         │  ├─ query/           # QueryClient ファクトリ・キャッシュ定数
         │  ├─ error/           # ApiError / ErrorResponse 骨格
         │  └─ env/             # 環境変数の読み取り・検証（zod）
         └─ generated/          # OpenAPI 生成物。手書き禁止・lint 対象外
```

-`components/ui`（アプリ固有）と `@repo/ui`（横断共有）を使い分け、横断で再利用するものだけ`@repo/ui`へ昇格する。
-OpenAPI 契約は frontend 内に置かず specs 側に置く。各サイト（app）が自分のサービスの`openapi.yaml`を相対参照して codegen する（§3.5）。

---

## 3. 構成要素の設計

### 3.1 モノレポ基盤

frontend workspace root（`<repo-root>/frontend`）の直下に`package.json`、`pnpm-workspace.yaml`、`pnpm-lock.yaml`、`mise.toml`を置く。frontend root に tsconfig は置かない。

-root の `package.json`は `"private": true`とし、共通 devDeps を寄せる。スクリプトは`pnpm -r run build/lint/test/typecheck/gen`と`pnpm --filter <app> ...`へ委譲し、ビルドロジックは root に書かない（package task を優先する）。
-`pnpm-workspace.yaml`に `apps/*`と`packages/*`を登録する。
-codegen（`gen`）を`typecheck`/ `build`の前段に置く依存順は、package 間の依存関係に基づく `pnpm -r`のトポロジカル実行で表現する（`pnpm -r`は既定で依存→依存元の順。`--parallel`を付けると順序保証は外れ、`--no-sort`で無効化できる点に注意）。
-将来 `apps/txdb`などを足しても、共有設定・UI をそのまま継承できる骨格にする。

#### Turborepo を当面不採用とする理由（§1 の判断）

-アプリ 1・将来サイト 2 つ程度の規模では、タスクの並列・依存順は pnpm の`--filter`＋ `pnpm -r`（トポロジカル順）で足りる。
-Turborepo の主な付加価値はタスク出力キャッシュ（ローカル／リモートキャッシュ）で、pnpm にはこれが無い。だが要る場面（CI のファンアウトが大きい・再ビルドが重い）はこの規模ではまだ来ておらず、過剰である。
-root → frontend/backend の polyglot 分割で Java(backend) には turbo は無関係（turbo は frontend のタスクしか見ない）。
-Docker のアプリ切り出しは `pnpm --filter <app> deploy`＋ Next `standalone`で代替する（§4.2）。

>再検討トリガー（満たしたら turbo 後付けを検討）: ① アプリ/パッケージが増えて CI が遅い ② リモートキャッシュを用意する判断がついた ③`pnpm -r`ではタスク依存・キャッシュの記述が苦しくなった。後付けは`apps/`/`packages/`構成を一切変えず`turbo.json`を 1 枚足し、root スクリプトを`pnpm -r`→`turbo run`に差し替えるだけ（低コスト・可逆）。だが今は持たない。

### 3.2 共有設定 package（`@repo/*`）

create-turbo の標準に合わせ、設定は用途別の専用 package に分割する（root に集約しない）。turbo 本体は使わないが（§3.1）、この命名・分割レイアウトは標準として踏襲する。設定を共有 package にまとめることで、1 名運用でも AI が「どの設定が正か」で迷わない。create-turbo の既定 scaffold が用意する config package は`@repo/typescript-config`と `@repo/eslint-config`の 2 つで、`@repo/tailwind-config`は`with-tailwind`例を参考にした自前追加である。

-`@repo/typescript-config`。tsconfig の base / nextjs バリアントを export する。各 app は`extends`のみ行い、`strict: true`は base 側で固定する。
-`@repo/eslint-config`。ESLint flat config を export する。§3.7 の機械ゲートをここに内包し、各アプリは import するだけで全ルールが効く。
-`@repo/tailwind-config`。Tailwind v4（CSS-first）前提で、`@theme`トークンを定義した共有 CSS テーマファイルを export し、各 app が`@import`する。v4 では設定の正は CSS であり（`@theme`でトークンを定義する公式推奨の方式）、JS preset ではなく共有 CSS を正とする。なお JS config の`presets:`自体は v4 でも `@config`経由でマージされ機能するため「未サポート」ではない（v4 で明示的に未サポートなのは `corePlugins`/ `safelist`/ `separator`の 3 つ）。任意値禁止などのトークン方針もここへ寄せる。

### 3.3 TanStack Query Provider

公式の Advanced Server Rendering ガイダンスに準拠する。server で QueryClient を使い回すとリクエスト間・ユーザー間でデータが混ざるため、基盤で固定する。本書はデータ取得を prefetch → hydrate を全画面の標準とする（§3.3.1）ので、QueryClient はその前提で構成する。

-`apps/master/src/lib/query/get-query-client.ts`に `makeQueryClient()`と `getQueryClient()`を置く。
  -サーバー: リクエスト毎に新規。公式 example `nextjs-app-prefetching`（§6）は server 判定（現行 example は `environmentManager.isServer()`。旧版は `isServer`の直 import）で毎回`makeQueryClient()`を返すファクトリ方式を採る。RSC ツリー内で 1 つの client を共有したい場合は React`cache()`で包む方式も公式ガイドが代替として挙げる（ただし client 全体を再シリアライズする欠点ありと明記）。本書は公式 example の isServer ファクトリを正とし、`cache()`採用は要検証とする。いずれもリクエスト間でデータが混ざらないことが要件。
  -ブラウザ: singleton。`if (!browserClient) browserClient = makeQueryClient()`。
-`lib/query/cache.ts`に `STALE`（staleTime）と`GC`（gcTime）の定数を置く（直書き禁止）。具体値は後決めとし、箱だけ用意する。グローバル既定は`makeQueryClient`に焼き、エンドポイント別の値は生成フックの呼び出し時に渡す。
-必須: グローバル `staleTime`を 0 より大きくする。prefetch → hydrate を標準にする以上、`staleTime: 0`（既定値）だとハイドレーション直後にクライアントがデータを stale と判断して即 refetch し、サーバーの prefetch が無駄になって二重フェッチになる。公式 Advanced SSR のコードコメントが "we usually want to set some default staleTime above 0 to avoid refetching immediately on the client"（2 行のコメントに分割して記載）と示すとおりで、公式トーンは「必須」ではなく推奨だが、prefetch を全画面標準とする本基盤では必須として扱い、`makeQueryClient`の`defaultOptions.queries.staleTime`に 0 超の既定を焼く（具体値は §3.3 ／`STALE`定数）。
-`app/providers.tsx`は `'use client'`で`QueryClientProvider`を置き、ブラウザ client を取得する。公式は「suspense 境界が無い場合に`useState`で初期化すると、初回レンダーで suspend した際に React が client を捨てる」ことに注意しており、ここでは`getQueryClient()`（ブラウザ singleton）で取得して二重管理を避ける。
-`app/layout.tsx`（Server）から `providers`を呼ぶ。

#### 3.3.1 データ取得の標準パターン（prefetch → hydrate）

取得モードは prefetch → hydrate を全画面の標準とする。Next.js Pod と Spring Boot API Pod は同一 EKS クラスタ・同一 AZ 内の別 Pod に配置し、prefetch 時のサーバー間往復はクラスタ内通信（数 ms。§4.2）で済むため、サーバー prefetch のコストは低く、初期表示・ページ遷移のリクエストウォーターフォールを平坦化できる（公式 Advanced SSR が推奨する理由）。

-境界判断を 1 パターンに固定。`'use client'`を書かない`page.tsx`は RSC。RSC は `page.tsx`の薄い prefetch テンプレに限定し、データ取得・更新ロジックは全て Client（`〇〇Client.tsx`）に集約する。実装者（AI 含む）が触るのは`〇〇Client.tsx`のみ・`page.tsx`はテンプレの定型、と AI エージェントの`'use client'`線引きを 1 パターンに縛れるのが利点（§4.3 の AGENTS.md に明文化）。
-queryOptions を生成物で共有。orval 生成の `getXxxQueryOptions()`を `page.tsx`の`prefetchQuery`と Client の `useQuery`の両方に渡すことで、queryFn / queryKey の一致を手で保たず生成物の共有で機械保証する（queryKey のブレ起因の二重フェッチを構造的に防ぐ。§3.5）。
-prefetch 対象は GET に限らない。検索・複雑な絞り込み body を取る read 用途の POST も prefetch → hydrate に乗せる。ただし orval は既定で GET→`useQuery`/ POST→`useMutation`を生成し、mutation には `getXxxQueryOptions()`が生成されず（かつ`useMutation`はフックで RSC から呼べず）prefetch テンプレに渡す共有生成物が存在しない。該当 operation を per-operation override で query 生成へ切り替えることで GET と同一経路に乗せる（具体は §3.5）。サーバー／クライアントが同一の`getXxxQueryOptions(body)`を共有する限り、POST でも同じ body を渡せば queryKey は構造的に一致し、二重フェッチ防止は GET と同じく成立する（`override.query.queryKey`を有効化しない方針＝下記・§3.5 とも矛盾しない）。
-prefetch は TanStack の `prefetchQuery(getXxxQueryOptions())`を使う。orval の prefetch ヘルパー（設定フラグ `override.query.usePrefetch`を有効化すると `prefetch<Operation>Query`が生成される）は、custom mutator 構成での挙動を一次情報で確認できないため使わず、`prefetchQuery`を正とする。`prefetchQuery`の queryFn はサーバーで走り、mutator の server 枝（絶対 base ＋ 認証注入。§3.5）を通る。実装者は`getXxxQueryOptions()`を server / client 両方へ渡すだけで、環境差は mutator が吸収する。
-生成物の配置は、`'use client'`を持つ hook ファイルと server から呼べる fetcher を分け、`client-only`が RSC に漏れないようにする。RSC への client-only 漏れは既知の課題だが（orval#1576。ただし当該 issue は not planned で、解決策は報告者のユーザー提案段階）、`tags-split`（tag 単位のファイル分割）だけで自動解決する仕組みではないため、ファイル分離の具体は要検証で詰める。
-Client フックは `useQuery`を既定とする（orval 生成フックを直接使用。`isPending`/`isError`で明示分岐）。
  >補足（要検証・採用余地）: 公式 example`nextjs-app-prefetching`の Client 側は`useSuspenseQuery`を採用（prefetch 前提でデータ存在保証・loading 分岐不要・Suspense 境界で扱う）。orval は両方生成しうるため、Suspense 境界での扱いにメリットを見込めれば画面単位で採用余地あり。本書では既定を`useQuery`とし、`useSuspenseQuery`採否は要検証で残す。

>重要な相互作用（orval#1986）: この標準は`getXxxQueryOptions()`を server（非フック文脈の`prefetchQuery`/`ensureQueryData`）でも使うことを前提にする。orval には、`output.override.query.queryKey`を有効化すると`getXxxQueryOptions`が`useXxxQueryOptions`のフック形に変わり、`prefetchQuery`/`ensureQueryData`等の非フック再利用が壊れる既知 issue（orval#1986、open）がある。したがって本基盤では `override.query.queryKey`を有効化しない（または #1986 の回避が取れるまで使わない）。client-first 時代は「影響軽微」だったが、prefetch を標準にする本構成では致命的になるため、orval 設定の固定事項とする（§3.5）。

```tsx
// app/.../page.tsx — RSC。薄い要素ゼロの定型テンプレ
exportdefaultasyncfunctionPage() {
  constqc=getQueryClient();                    // server: リクエスト毎 new
  awaitqc.prefetchQuery(getUsersQueryOptions());  // orval 生成物（Client と同一）
  return(
    <HydrationBoundarystate={dehydrate(qc)}>
      <UsersClient/>
    </HydrationBoundary>
  );
}
```

```tsx
// app/.../UsersClient.tsx — ぜんぶ Client。実装者はここだけ触る
"use client";
exportdefaultfunctionUsersClient() {
  const{data}=useQuery(getUsersQueryOptions());// page.tsx と同一の生成物
  // フィルタ・ソート・mutation は全てここ
}
```

### 3.4 BFF キャッチオール proxy（骨格）

`app/api/[...path]/route.ts`で `/api/`を受け、認証トークンの付与とヘッダ整形を行って Spring Boot へ透過する骨格を置く。基盤時点は素通し proxy とトークン付与の差し込み口までとし、アグリゲーションやマスタ固有の整形は後続とする。

-multipart / binary 透過の考慮点を TODO コメントで明示する（レビュー指摘 B の入口）。ブラウザは backend の URL もトークンも知らない。
-秘匿情報はサーバー側に閉じ、`lib/env`で検証して読む（§4.5）。

### 3.5 OpenAPI codegen パイプライン

入力は specs 内の対応サービス仕様の `openapi.yaml`で、各サイト（app）が自分のサービスのものを相対参照する。backend が提供するまでは仮置きスキーマでパイプラインを通す。契約本体を specs に置くのは、サービス仕様の 1 アウトプットとして管理し、frontend と backend が同じ 1 本を参照するためである（`contracts/`案は見送り）。

生成は `pnpm gen:api`（`pnpm --filter master gen`等の workspace スクリプト）で行い、orval（設定は`apps/master/orval.config.ts`）が `apps/master/src/generated/`に出力する。orval に一本化し、OpenAPI 1 本から次を生成する（選定の経緯は §1.1）。

| 対象 | orval 出力 | 備考 |
| --- | --- | --- |
| 型 | TS 型 | |
| client | fetch ベース client | custom mutator で BFF（`/api`）経由・トークン付与に通す（§3.4） |
| hooks | useQuery / useMutation、queryKey / queryOptions を含むフル生成 | 生成フックを直接使う。orval の`client: 'react-query'`モードを採用するため client と hooks が一括生成される（`'fetch'`/`'axios'`モードなら型＋client のみも生成できるが、本基盤は生成フックを正とする）。既定の HTTP verb 対応は GET→`useQuery`/ POST・PUT・PATCH・DELETE→`useMutation`。`getXxxQueryOptions()`は `page.tsx`の prefetch と Client の useQuery の両方に渡す（§3.3.1）。read 用途の POST は per-operation override で query 生成へ切り替える（下記）。エンドポイント別の staleTime / gcTime はフック呼び出し時に`STALE`/ `GC`定数を渡す（§3.3）。`getXxxQueryKey`はそのまま invalidate にも流用する。横断ロジック（楽観更新・select 変換など）が要る所だけ feature 層の薄い hook で包む |
| MSW | MSW handlers | スキーマ整合のモックを自動生成する（§3.6） |

-Zod は生成しない。生成 TS 型を土台に、入力バリデーション用 Zod は手書きする（型から起こして乖離を防ぐ）。配置は生成物ではないので`src/generated/`の外（features 側のバリデーション層）に置く。
-API レスポンスの実行時検証は行わない。生成 TS 型はコンパイル時のみで、契約と実レスポンスのズレは検知しない。マスタ CRUD では許容し、必要になった時点で response 用 Zod を足す。
-fetch は mutator 1 か所のみ。実 `fetch()`は手書きの mutator（`src/lib/api/`）だけが行い、生成 client・hooks はすべてここを通る。これで「fetch 直書き禁止」（§3.7）が経路として守られる。
  -mutator は環境対応の 1 本にする（`typeof window === 'undefined'`で server / client を分岐）。orval は base URL を設定側の`output.baseUrl`でも mutator 内でも持てるが、本基盤は環境差（server / client の base・認証）を 1 か所に吸収するため mutator 内に base URL を持たせ、分岐もここで行う。サーバー専用 mutator を別生成する案は保守対象が増えるため採らない。
  -client 枝（ブラウザ）: base は `/api`（相対・same-origin）。backend の絶対 URL・トークンはブラウザに出さない（`NEXT_PUBLIC_*`に backend URL を置かない）。`credentials:"include"`で session cookie を送るだけで、backend 用トークン付与は BFF の責務。
  -server 枝（RSC prefetch・§3.3.1）: 相対`/api`はサーバーでは base が無く解決できず throw する。ブラウザの cookie 自動送出も効かない。したがって server 枝は 絶対 base ＋ 認証の明示注入が要る。方式は A 案（BFF 経由で incoming cookie を転送）で確定する。すなわち server 枝の絶対 base は自分の BFF（`/api`）を指し、RSC 側で `cookies()`から取り出した session cookie を per-call options で mutator に渡して BFF へ転送するだけとする。トークン付与・redis 等からのセッション解決は BFF 内に閉じる（client 枝と同じく BFF が唯一のトークン付与点。§4.5）。B 案（backend 直叩き＋トークン）は、Next 側でも backend 用トークンを扱うことになり「トークン付与は BFF の責務・ブラウザ／Next にトークンを出さない」方針に反するため却下（§1 検討事項）。認証の中身（cookie の具体仕様・セッションストア・トークン交換）は認可設計（レビュー指摘 C・後続）で詰めるが、基盤の seam（絶対 base ＋ cookie 転送の per-call options）と疎通スモークは A 案で確定済みのため、MSW モックで`/api`経由の prefetch 動作を検証できる（§5 DoD）。
  -認証値は mutator ファイル内で `next/headers`を読まない。mutator（やその依存チェーン）内の `await import('next/headers')`が orval の codegen（mutator の export 解析）を壊す既知問題があるため（orval#2701 / #1634、いずれも closed・対応済み）、RSC 側で`cookies()`から解決し、mutator の per-call options 引数で注入する。
  -mutator の責務は、JSON ヘッダ・`AbortSignal`の付与、非 2xx を`ApiError`（§3.4、`lib/error`）へ変換して throw すること（TanStack Query が捕捉する）、multipart は Content-Type 非固定で FormData を透過することである。
  -BFF の責務は、トークン付与・ヘッダ整形・（将来の）aggregation である。MSW は mutator が叩く`/api/...`（client 枝）を横取りするため、本物もモックも同じ口で差し替わる（§3.6）。
-read 用途の POST を prefetch する手当て: 検索・複雑な絞り込み body を取る POST 等、「実質 read だが POST」のエンドポイントは、該当 operation だけ per-operation override で query 生成へ切り替える。これで`getXxxQueryOptions()`が生成され、GET と同一の prefetch → hydrate 経路（§3.3.1）にそのまま乗る。RSC で POST を発行できないわけではなく（mutator の`fetch()`はサーバーでも POST を発行できる）、問題は orval が既定で POST を mutation として生成し prefetch に渡せる共有生成物が無いことなので、生成物の形を query へ寄せて解消する。
  ```ts
  // orval.config.ts
  override:{
    operations: {
      searchUsers: {                 // ← read 用途の POST の operationId
        query: {useQuery:true,useMutation:false},  // mutation 生成を抑止し同名関数衝突を回避
      },
    },
  }
  ```
  -グローバル `override.query.useQuery: true`は POST に効かない既知バグ（orval#1723 / #2376。fix は v8.10.0 / PR#3323）があり、かつ全 POST を一律 query 化すると本物の mutation まで巻き込むため、per-operation override を正とする（全バージョンで有効・対象を限定できる）。
  -「read な POST」と「write な POST」を区別する手段が OpenAPI には無いため、対象 operation の判定基準（operationId を`orval.config.ts`に列挙する／spec 側で tag・`x-`拡張に印を付けて拾う）を規約として決める。これが本手当ての唯一の実コストで、基盤の固定事項とする。
  -`useSuspenseQuery`も同様にグローバルは GET 限定で、per-operation override で個別の POST に展開できる（§3.3.1 の useSuspenseQuery 採否＝要検証に整合）。
-`getXxxQueryKey`の invalidate 流用は安定だが、`override.query.queryKey`有効時に`getXxxQueryOptions`がフック形へ変わり `ensureQueryData`等での非フック再利用が効かなくなる既知 issue（orval#1986、open）がある。本書は prefetch → hydrate を標準とし`getXxxQueryOptions()`を server prefetch（非フック）でも使うため、`override.query.queryKey`は有効化しない（有効化すると`prefetchQuery`/`ensureQueryData`が壊れる。§3.3.1）。上記 read 用途 POST の query 化は per-operation override で別個に行い、`queryKey`override には依存しないため両者は両立する。
-drift 検出は CI で行い、「再生成結果 == コミット済み生成物」を`git diff --exit-code`で検証する。
-生成物は lint / フォーマットの対象外とし、§3.7 の`no-restricted-imports`で経路だけ固定する。MSW handlers も生成物として手書き禁止扱いとする。
-配置は当面アプリ内とし、複数アプリが同一スキーマを共有する必要が出たら`packages/api`へ昇格する。

### 3.6 テスト

テストランナーは Vitest、コンポーネント描画は React Testing Library（RTL）、UI カタログは Storybook、ブラウザフローは Playwright を用いる。RTL は Vitest の代替ではなく、その中でコンポーネントを描画・操作するライブラリで、両者は競合しない（「Vitest に寄せる」時点で中身は RTL を使う）。`jsdom`を明示し、`@vitejs/plugin-react`を使う。

API モックは全層で MSW に統一し、orval が `src/generated/`に出す MSW handlers（生成型と整合）を共用する。モック定義を二重に持たない。生成 handler が保証するのは契約の形状であって業務的に意味のあるデータではない（中身は faker のランダム値）ため、現実的なデータが要る所は生成 handler を決定論的な fixture で override する層を別に持つ。

>このテスト群が保証する範囲（重要な前提）。MSW がブラウザのネットワーク層で`/api/*`を横取りするため、フロント側のコードパス（mutator → TanStack Query → hooks → UI）はほぼ本番どおり走り、かつ生成モックは契約形状と必ず一致する。ただし保証されるのは「フロント ↔ OpenAPI 契約」の整合までで、次の 2 つはこの層では検知できない。(1) BFF のロジック（トークン付与・ケース変換 §4.4・aggregation）——ブラウザ横取りは Next の Route Handler に届かない（後述）。(2)「OpenAPI 契約 ↔ 実 backend 実装」のズレ——レスポンスの実行時検証はしない（§3.5）。これらは後続のライブ E2E（と backend 側の契約テスト）に委ねる。

#### テスト層の責務分担

各ツールが「コンポーネント＋MSW」を実行できてしまうため、責務を固定し、同じテストを重複して書かない。

| 層 | ランナー / ツール | 何を | 置き場所 |
| --- | --- | --- | --- |
| ロジック / hooks / Zod | Vitest（+ MSW node） | 入出力・分岐・エラー整形 | アプリ / `@repo/lib`|
| コンポーネント状態 | Vitest + RTL（story を import） | loading / error / empty・操作 | story と同居 |
| UI カタログ | Storybook（描画のみ） | 目視確認・AI 生成 | 共通は `@repo/ui`、固有は app |
| 機能リグレッション | Playwright（撮影しない） | ユーザーストーリー単位のフロー（サイトのデグレ） | `apps/*/tests/e2e`|
| ビジュアル回帰（検討） | story をスナップショット | 見た目デグレ検知（要否・対象を検討） | story（採用時） |

-Storybook は「作った UI コンポーネントをレンダリングするだけ」とし、凝ったカタログ整備は必要時に行う（AI で起こせる）。
-story を単一ソースにする。コンポーネントの状態（loading / error / empty 等）を story として 1 回だけ書き、それを ① Vitest が import してテスト実行（portable stories /`@storybook/addon-vitest`）、② Storybook が描画してカタログ、③（ビジュアル回帰を採用する場合）スナップショット、と 1 つの story を使い回す。状態定義の二重持ちを避ける。
-リグレッションの本命は機能リグレッション（Playwright）で、見たいのは「サイト自体のデグレ」（フロー・画面が壊れていないか）である。Playwright はフロー（アプリ単位）の機能回帰のみを担い、画面は撮影しない。
-ビジュアル回帰（見た目のスナップショット差分）を入れるかは検討とする。リグレッションで本当に検知したいのはサイト自体のデグレであり、共通部品（`@repo/ui`）の見た目を撮ってもサイトのデグレ検知としては効果が薄い。一方サイト固有の画面を撮るなら機能回帰の Playwright と対象が重なる。対象（共通部品か / サイト固有画面か）・ツール・粒度を含めて、UI が固まってから要否を判断する。仮に採用する場合でも、横断する共通部品は `@repo/ui`の story として 1 回だけ撮り（アプリごとに撮り直さない）、Playwright のフロー撮影で代替はしない。

#### MSW の起動口（レイヤ別）

-Vitest（node）: `setupServer`（MSW node）で生成 handlers を起動する。
-Storybook: `msw-storybook-addon`で同じ生成 handlers を起動し、story 単位で override する。
-ローカル手動開発（ブラウザ）: `setupWorker`（MSW browser worker、`public/mockServiceWorker.js`）を`NEXT_PUBLIC_API_MOCKING`で env ゲートして起動する。フラグが無効なら`worker.start()`を呼ばない。MSW は本番ビルドで自動的に剥がれるわけではなく、`worker.start()`が env で呼ばれないことで止まる。
-Playwright E2E: 公式バインディング `@msw/playwright`を使い、テスト層（内部的に Playwright の `page.route`）から生成 handlers を注入する。アプリのビルドにモック起動を焼き込まないので、E2E 専用のモックビルドを別途用意せずに済む。

#### E2E の実行モード

MSW は「本番ビルドで自動的に除外される」わけではない。モックが止まるのは `worker.start()`（または `@msw/playwright`の注入）が env フラグで呼ばれないからであって、ビルドがモックを剥がすからではない。この前提で 2 モードに整理する。

| モード | モック | 実行場所 | backend | BFF proxy | 基盤での扱い |
| --- | --- | --- | --- | --- | --- |
| モック E2E（hermetic） | MSW（`@msw/playwright`注入） | ローカル＋PR CI（§4.1） | 不要 | 経由しない（後述） | 基盤の対象。疎通スモークのみ |
| ライブ E2E | なし | GitHub Actions →デプロイ済み環境 | 必要 | 経由する | 後続。backend・デプロイに依存 |

-基盤時点はモック E2E のみを通す。CI には backend が無いため、`pnpm -r e2e`（§4.1）はモックモードで動く。
-ライブ E2E は後続。検証／本番環境へデプロイ後、`@msw/playwright`の注入を外して Playwright をデプロイ済み URL に向ければ実 BFF→backend を叩く。切替は「テスト層でモックを注入するか否か」だけで、別ビルドは不要。
-BFF バイパスの明示: MSW のブラウザ横取り（worker /`page.route`）はブラウザ内で `/api/*`を捕えるため、リクエストは Next の BFF Route Handler に届かない（§3.4/§3.5 の `/api`口で差し替わる）。本基盤は E2E に公式 `@msw/playwright`（内部は`page.route`）を使い、MSW のブラウザ worker は起動しないため`serviceWorkers: 'block'`の調整は不要（service worker が`page.route`より先に介入して横取りが効かなくなるのは、MSW のブラウザ worker を Playwright と併用する旧来構成の話で、その場合に限り`serviceWorkers: 'block'`等が要る）。したがってモック E2E は BFF proxy を通らない。素通し proxy の段階では許容するが、将来 BFF に集約ロジック（aggregation・ケース変換 §4.4）を載せて E2E でそこも検証したくなったら、ブラウザ横取りではなく`instrumentation.ts`の `register()`で MSW node を起動し、BFF の backend 向け outbound を横取りする方式へ切り替える（この方式なら BFF を経由する）。
-Playwright は本番相当ビルド（`next build`→`next start`）に対して実行し、`next dev`は使わない。

#### 基盤での扱い

-基盤時点の中身は「ハーネスが動く」ことを示す最小スモークのみとする（node server / browser worker / Storybook /`@msw/playwright`の起動配線と疎通スモーク）。マスタの CRUD E2E は後続とする。
-リアルなマスタデータが要る本格モック（生成 handler の override による決定論 fixture）は後続とする。
-ビジュアル回帰は基盤では採否未確定（検討）とする。要否・対象・ツール（Chromatic /`@storybook/test-runner`等）の判断は UI が固まってからとし、基盤時点では配線・baseline・CI ゲートのいずれも入れない。story を単一ソースに揃えてあるため、後から採用する場合も既存 story をスナップショット対象に足すだけで済む。

### 3.7 機械ゲート（ESLint / tsc）

次は `@repo/eslint-config`/ `@repo/typescript-config`に入れ、全アプリへ一括適用する。

| 守る対象 | 強制手段 |
| --- | --- |
| `fetch`直書き禁止 | ESLint `no-restricted-syntax`（`window.fetch`/`globalThis.fetch`経由も含め確実に塞ぐ。`no-restricted-globals`は裸の`fetch()`しか捕まえないため `checkGlobalObject: true`併用か `no-restricted-syntax`を正とする） |
| `src/generated/`経路固定・手書き禁止 | ESLint`no-restricted-imports`|
| Server Actions 禁止（`'use server'`） | ESLint とレビュー |
| Rules of Hooks・依存配列の網羅（`exhaustive-deps`） |`eslint-plugin-react-hooks`|
| Tailwind 任意値（`w-[137px]`）禁止 | ESLint`eslint-plugin-tailwindcss@^4`（Tailwind v4 対応版）の`tailwindcss/no-arbitrary-value`|
| 文言ハードコード禁止 | `eslint-plugin-i18next`の`i18next/no-literal-string`（基盤では warn から開始してよい） |
| 型安全 | `tsc --noEmit`（`strict: true`） |

-`tailwindcss/no-arbitrary-value`は recommended preset に含まれないため明示的に有効化する。任意値を全面禁止したいのはこの`no-arbitrary-value`であり、recommended に含まれデフォルト ON の同名類似ルール`no-unnecessary-arbitrary-value`（冗長な任意値の書き換え）とは別物なので取り違えない。eslint-plugin-tailwindcss の Tailwind v4 対応版（`^4`）は新しいので、共有 CSS テーマ（§3.2）構成で実際に発火することを DoD で確認する。
-`fetch`直書き禁止の唯一の例外は mutator（§3.5）で、そのファイルだけ override で許可する。
-`prefetchQuery`/`ensureQueryData`の非フック再利用を守るため、orval は`override.query.queryKey`を有効化しない（§3.3.1 / §3.5。機械ゲートではなく orval 設定側の固定事項）。

---

## 4. 横断的関心事

### 4.1 CI（GitHub Actions）

`push`と `pull_request`で次を実行する。pnpm workspace で並列・フィルタ実行し、1 つでも落ちたらマージ不可とする。

1.`pnpm install`
2.`pnpm -r gen:api`（codegen）→ `git diff --exit-code`（drift 検出）
3.`pnpm -r typecheck`（`tsc --noEmit`）
4.`pnpm -r lint`
5.`pnpm -r test`（Vitest）
6.`pnpm -r build`（`next build`）
7.`pnpm -r e2e`（Playwright・本番相当ビルドに対して、MSW モックモード。§3.6）

CI はエージェントの実行環境と独立に動かし、AI のローカルでの「動いた」を最終関門で検証する。上記は frontend ジョブ（`frontend/`を working-directory に実行）である。

>Storybook は基盤時点では「story がビルドできる（`build-storybook`が通る）」ことの確認までを CI に含める。ビジュアル回帰は採否未確定（検討）のため CI には一切入れない（§3.6「基盤での扱い」）。`pnpm -r test`（step 5）は portable stories 経由で story をテストとして実行する分も含む。

>未決: polyglot モノレポでの`.github`の置き場と、frontend / backend をパスフィルタで出し分ける構成。repo-root に 1 つの`.github`を置き path filter で振り分けるのが定石で、要検討とする。

### 4.2 デプロイ前提

`apps/master/Dockerfile`を multi-stage・standalone で用意する。アプリ単位の切り出しは`pnpm --filter master deploy <dir>`を用いる（pnpm 標準。`turbo prune`は使わない＝§3.1）。

-`pnpm --filter <app> deploy`の前提（確定: `inject-workspace-packages=true`）: pnpm v10 では `deploy`は既定で `inject-workspace-packages=true`を要求し、無ければ`--legacy`（または `force-legacy-deploy=true`）が要る（この回避フラグは pnpm v10.2.1 以降で導入。それ以前の v10 では回避できない）。本基盤は`inject-workspace-packages=true`で確定する（社内パッケージをシンボリックリンクではなくハードリンクで実体同梱し、リンクに依存しない持ち運び可能な成果物になる。Docker 向きで v10 の既定方向）。`.npmrc`に明記する。
  -⚠️ 既知バグ（要監視）: `@repo/*`同士が依存したり `catalog:`を使う場合、ネストした`workspace:`/`catalog:`を書き換えない不具合（pnpm#9800〈closed〉、および catalog 使用時に deploy が壊れる pnpm#8950 等）で生成物が壊れ得る。現状の`apps/master`＋ 浅い `@repo/*`依存（master → `@repo/*`の一段、catalog 不使用）なら踏まない。共有 package を相互依存させる・catalog を導入する段で再確認する。
-`pnpm deploy`と standalone の役割分担: ランタイムイメージで `node_modules`を不要にする主役は Next の `output: 'standalone'`（`@vercel/nft`のファイルトレース）であり、`pnpm deploy`はその前段、ビルドコンテキストを 1 アプリ分に切り出す役と位置づける。最終的には builder ステージで install/build し、`.next/standalone`をランタイムイメージへコピーする。
-`outputFileTracingRoot`を `next.config.ts`に設定する（Next 15 で experimental から昇格したトップレベル設定。例:`path.join(__dirname, '../../')`= `frontend/`）。pnpm workspace では `node_modules`が workspace root にホイストされるため、未設定だと standalone のトレース基準が`apps/master`配下に閉じ、root の依存を取りこぼした不完全な成果物になり得る（顕在化はランタイムで起きることがある）。公式の表現は条件付き（"can set"）だが、依存が workspace root にホイストされる pnpm 構成では実質必須となる。pnpm の symlink 構造がトレース出力で問題化する場合は store 解決・`cp -rL`等で対処する。
-standalone は `.next/static`と `public/`を含まないため別途コピーする。
-画像最適化: Next.js 15 以降は `next start`/ standalone で `sharp`を自動使用するため手動インストールは不要（本基盤は Next 15+ 前提。なお公式の`sharp-missing-in-production`ページは更新漏れで古い記述が残る）。14 以前を使う場合のみ runner に`sharp`を入れる。
-デプロイ topology（prefetch の前提）: Next.js Pod と Spring Boot API Pod を同一 EKS クラスタ・同一 AZ に別 Pod で配置する。prefetch（§3.3.1）時のサーバー間往復はクラスタ内通信（数 ms）で済み、回線品質の影響を受けにくいため prefetch → hydrate を全画面標準にしてもコストは低い（別 AZ にまたがると微小なレイテンシ増）。
-静的アセットは CDN / ALB 前段を推奨する。
-K8s では Pod ごとにキャッシュが分かれる。クライアントファーストで SSR / ISR キャッシュをほぼ使わないため影響は小さい。
-`.dockerignore`を用意する。

### 4.3 規約・AI ルールファイル

本書では frontend サブツリー root（`<repo-root>/frontend/`）に置く分のみ確定させる。AGENTS.md はクロスツール標準、`.claude/`は Claude Code 公式の慣習に従う。

-`AGENTS.md`（`frontend/`）。基盤の「やる／やらない」を明文化する（§3.3.1 の`'use client'`線引き＝「`page.tsx`は薄テンプレ、取得・更新は`〇〇Client.tsx`」も明文化）。Codex / Cursor / Copilot なども読む。`apps/master/AGENTS.md`を置けばそのサブツリーで上書きする（最も近いものが勝つ）。基盤時点は`frontend/`に 1 つでよい。
-`CLAUDE.md`（`frontend/`）。1 行目を`@AGENTS.md`にして import し、重複を避ける。
-`.claude/`（`frontend/`）。`skills/<name>/SKILL.md`（スキル）、`commands/*.md`（スラッシュコマンド）、`agents/*.md`（サブエージェント定義）、`settings.json`（共有設定）を置く。`settings.local.json`は個人設定で `.gitignore`する。
-コーディング規約の本体は markdown に書かず、2 層で frontend root に共有して app ごとに複製しない。機械強制ルール（フォーマット / lint / 型）は`@repo/eslint-config`/ `@repo/typescript-config`（と prettier）に置き、各 app は extends・import のみ行う。散文ガイドラインは`frontend/CONTRIBUTING.md`（人間向け）と `frontend/AGENTS.md`（エージェント向け）に置く。
-`docs/conventions/`には上記で縛れない補足の散文のみを置く（queryKey 命名例・粒度の説明など）。

>未決: frontend / backend / specs を跨ぐ repo-root 版（横断 AGENTS.md / CLAUDE.md /`.github`）を別に持つか、各サブツリーにだけ置くか。AGENTS.md は「最も近いものが勝つ」ため、repo-root に薄い横断版を置き各サブツリーで上書きする構成も取れる。

### 4.4 ケース変換（snake ⇄ camel）

ドメイン型は camelCase で確定している。snake ⇄ camel の境界変換は BFF で寄せることを推奨する（集約点で一元化し、generated と mutator を dumb に保つ）。基盤の BFF が素通しのままなら、暫定で mutator 変換も可とする。具体方針は実スキーマ確定時に決める。

### 4.5 セキュリティ

秘匿情報はサーバー側に閉じ、ブラウザには backend の絶対 URL もトークンも出さない（§3.4、§3.5）。ブラウザは same-origin の`/api`に対して session cookie を送るだけで、backend 用のトークン付与は BFF の責務とする。環境変数は`lib/env`で検証して読む。

---

## 5. 完了の定義（DoD）

-[ ] `frontend/`から `pnpm install && pnpm -r gen:api && pnpm -r typecheck && pnpm -r lint && pnpm -r test && pnpm -r build && pnpm -r e2e`がローカルと CI の両方で緑になる。
-[ ] 共有 config package（`@repo/typescript-config`/`@repo/eslint-config`）を `apps/master`が extends・import し、機械ゲートが効いている。
-[ ] サンプルまたは実 OpenAPI スキーマから `src/generated/`が生成され、drift 検出が CI で機能する。
-[ ] TanStack Query Provider が §3.3 のとおり（server はリクエスト毎 new / browser singleton）で動く。
-[ ] prefetch → hydrate テンプレ（§3.3.1）が疎通ページで動作し、ハイドレーション後にクライアントが即フェッチしない（`staleTime>0`既定が効いている）。server 枝の prefetch は A 案（BFF 経由で cookie 転送・§3.5）の seam を通し、検証は MSW モックで行う（本番認証注入の中身はライブ E2E＝後続）。
-[ ] BFF proxy 骨格経由で health チェックが通る（ブラウザは`/api`のみ叩く）。
-[ ] `pnpm --filter master deploy`＋ `docker build`が通り、コンテナ起動後に health が応答を返す。
-[ ] `AGENTS.md`/ `CLAUDE.md`が配置され、主要規約と機械ゲートが二重化されている。

>この DoD を満たした時点で、A・B・C を合意のうえマスタ機能の実装に進む。

---

## 6. 参照（外部の一次情報）

本書の内容は本書だけで完結する。設計判断の裏付けとして、必要なら次の公式ドキュメントを参照する（いずれも外部・公開資料）。

-TanStack Query — Advanced Server Rendering（QueryClient の server / browser 分離・`staleTime>0`必須・hydration の正旨）
-TanStack Query — 公式 example `nextjs-app-prefetching`（prefetch → hydrate の参照実装・`page.tsx`テンプレの正旨。https://github.com/TanStack/query/tree/main/examples/react/nextjs-app-prefetching ）
-Next.js — Self-Hosting Guide（standalone / コンテナ / K8s の注意点）、`outputFileTracingRoot`
-orval — 公式ドキュメント（OpenAPI → 型 / client / TanStack Query / MSW、mutator・mock 設定、`output.baseUrl`）。queryKey override は orval#1986（open）、mutator 内の`next/headers`動的 import 問題は orval#2701 / #1634（closed・対応済み）、RSC への client-only 漏れは orval#1576（not planned）、read 用途 POST の query 化（per-operation override）とグローバル`useQuery`が POST に効かないバグは orval#198 / #1723 / #2376（#2376 は v8.10.0 で fix）を参照
-MSW — Browser / Node Integration（conditional start・service worker）、`@msw/playwright`（E2E 注入）／Playwright — Network（`page.route`・service worker）
-Storybook — 公式（portable stories、`@storybook/addon-vitest`、`msw-storybook-addon`、`@storybook/test-runner`）／RTL — Guiding Principles
-openapi-typescript — 2026 Roadmap（openapi-fetch のメンテナンスモード移行）
-pnpm — Workspace / `pnpm -r`（recursive・topological）／`pnpm deploy`（`inject-workspace-packages`・`--legacy`）。Turborepo は将来後付けの候補（§3.1）
